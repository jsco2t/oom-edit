# Evidence — Task 005

## Result

PASS — spell work waits for five seconds of input idleness and unaffected
diagnostics remain visible throughout repeated locally-safe edits.

## Implementation

- Changed the event-loop spell idle threshold from 150 milliseconds to exactly
  five seconds while preserving the existing sampled-input clock boundary and
  cooperative slice limits.
- Retained and byte-shifted only diagnostics and exclusions outside locally
  invalidated lines, including when a prior replacement scan is still pending.
- Discarded obsolete partial scan cursors and restarted over the union of
  affected ranges; full/context-changing invalidations remain conservative.
- Added source/rendered decoration, exact timing, reset, stale-cursor, and fresh
  full-scan equivalence coverage.

## Validation

- `cargo test -p oom-edit --offline --locked spell_idle_delay` — PASS
- `cargo test -p oom-edit --offline --locked timeout_requires_full_idle_delay` — PASS
- `cargo test -p oom-edit-core --offline --locked repeated_local_spell_invalidation` — PASS
- `cargo test -p oom-edit-core --offline --locked --test spell_session` — PASS (21 tests)
- `make check` — PASS

## Review

PASS — retained diagnostics always describe unchanged source, partial results
never publish after a newer edit, and bounded idle work and input precedence are
unchanged.
