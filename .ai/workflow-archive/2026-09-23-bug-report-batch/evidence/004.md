# Evidence — Task 004

## Result

PASS — Normal-mode `Space m` inserts the exact default YAML front matter once.

## Implementation

- Added a core session operation that atomically prepends the exact default
  template, preserves the logical cursor, refreshes derived caches, and remains
  one undo/redo transaction.
- Registered `default-front-matter` as the single source for dispatch, palette,
  hints, and which-key metadata, bound to `Space m` in Normal mode.
- Added deterministic success/already-present feedback and synchronized scroll
  and spell-gutter invalidation with the edit.
- Documented the new binding and exact inserted template.

## Validation

- `cargo test -p oom-edit-core --offline --locked default_front_matter` — PASS
- `cargo test -p oom-edit --offline --locked front_matter_command` — PASS
- `cargo test -p oom-edit --offline --locked command::registry` — PASS
- `make check` — PASS

## Review

PASS — the mutation uses the canonical `LiveDocument` gateway, the registry is
the only command declaration, third-party types do not cross the public facade,
and existing or malformed front matter is never overwritten.
