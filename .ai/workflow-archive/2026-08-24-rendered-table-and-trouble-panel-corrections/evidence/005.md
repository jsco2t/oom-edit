# Evidence — Task 005

## Implementation

- Implemented by: main
- Attempt count: 3

## Files changed

- `README.md`
- `crates/oom-edit-core/src/rendered/mod.rs`
- `crates/oom-edit-core/src/rendered/table.rs`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`
- `crates/oom-edit-core/src/style.rs`
- `crates/oom-edit-core/tests/goldens/kitchen_sink.txt`
- `crates/oom-edit-core/tests/goldens/vw9_tables.txt`
- `crates/oom-edit-core/tests/public_api.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/config.rs`
- `crates/oom-edit/src/lib.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/snapshot_tests.rs`
- `crates/oom-edit/src/theme.rs`
- `crates/oom-edit/tests/snapshots/rendered_table.txt`

## Acceptance criteria

- [x] Alternating table-row backgrounds are completely removed.
  - Evidence: the core row role, TUI theme slot, configuration/runtime plumbing, renderer composition, README entry, public API guard entries, and striping tests were removed. Older configuration text is covered by an ignored-and-not-serialized compatibility regression.
- [x] Tables render exactly one boundary between adjacent body rows.
  - Evidence: table-builder coverage checks zero through three body rows and verifies exactly `N-1` boundaries, with no boundary after the header or before the bottom border.
- [x] Wrapped logical rows receive a single boundary after their final continuation.
  - Evidence: a long-cell layout regression checks boundary placement after the complete logical row and before the next source row.
- [x] Boundaries fill current column interiors with faint ASCII dashes while remaining inside table borders.
  - Evidence: generated `│` borders retain ordinary table styling, every allocated interior cell uses `SemanticStyle::Muted` `-` glyphs, row widths match, and every built-in theme/capability tier resolves the dash carrier to DIM.
- [x] Boundary geometry recomputes when terminal width changes.
  - Evidence: a non-vacuous 80-to-100-column regression forces different column allocations and checks boundary widths and content change with the table.
- [x] Boundaries remain synthetic and source-safe for selection, navigation, and operators.
  - Evidence: every boundary atom has `source: None`; character, line, and block selection/yank regressions exclude the rendered separator from source ranges and clipboard payloads; navigation across the physical boundary preserves canonical source ownership deterministically.
- [x] Public guards, goldens, snapshots, and the earlier retained behavior are current.
  - Evidence: curated API tests, core table goldens, the TUI table snapshot, word wrapping, wrapped selection, and Trouble coverage all pass in the complete suite.

## Main-thread review

Reviewed logical-row ownership, wrapped-row placement, allocated display width, source provenance, semantic styling across all theme tiers, selection/operator projection, navigation, removed configuration compatibility, and intentional snapshot changes. Review corrected synthetic top-border metadata to retain its nearest-header convention. Two early versions of the new three-shape selection regression over-constrained internal display-row painting; attempt 3 verifies the actual source and operator contract without changing established selection visuals.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make test` | PASS (attempt 3) |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

None
