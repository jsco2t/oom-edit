# Evidence — Task 004

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/overlay/trouble.rs`
- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/snapshot_tests.rs`
- `crates/oom-edit/tests/snapshots/trouble.txt`

## Acceptance criteria

- [x] One- and multi-digit positions align location, severity, provider, and message columns.
  - Evidence: a display-cell-aware column regression covers mixed position and severity widths; the Trouble snapshot shows the corrected alignment.
- [x] Alignment remains stable across scrolling, refresh, selection, narrow terminals, and terminal progress states.
  - Evidence: widths are derived from the complete current snapshot before viewport slicing; focused scrolling coverage includes the widest offscreen row, while existing refresh/footer/narrow tests pass.
- [x] Trouble hides the terminal cursor over Normal, Select, and Insert while preserving the selected-row carrier.
  - Evidence: App-level TestBackend coverage asserts cursor invisibility and the reversed selected marker in all three modes.
- [x] Closing or completing a valid jump restores ordinary cursor presentation on the next frame.
  - Evidence: Escape restoration is checked for all three modes, and the existing atomic jump regression now checks hidden-before and visible-after states.
- [x] Existing Trouble routing and jump behavior remain unchanged.
  - Evidence: exclusive modal input, stale jump, refresh, navigation, and successful jump tests all pass.
- [x] Snapshot and focused regressions cover corrected output.
  - Evidence: `golden_trouble`, column alignment, offscreen-width stability, modal cursor ownership, and the complete suite pass.

## Main-thread review

Reviewed display-cell measurement, complete-snapshot ownership, scrolling and footer capacity, clipping behavior, severity and selection styling, modal routing, and frame-global cursor semantics. Cursor suppression is performed by the background screen before overlay rendering, preserving the ability for a future cursor-owning overlay to request its own position.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

None
