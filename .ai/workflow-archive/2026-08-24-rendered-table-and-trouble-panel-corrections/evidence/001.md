# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/rendered/table.rs`
- `crates/oom-edit-core/tests/goldens/vw9_tables.txt`
- `crates/oom-edit/tests/snapshots/rendered_table.txt`

## Acceptance criteria

- [x] Ordinary prose wraps at the last fitting word boundary.
  - Evidence: focused `table_prose_wraps_before_the_next_word` and rendered-table integration tests assert the requested sentence breaks before `content`.
- [x] Exact-fit words remain together and overlong tokens hard-wrap safely.
  - Evidence: table chunker unit cases cover exact fits, overlong ASCII, width-one CJK, and an indivisible emoji display group.
- [x] Rendered rows retain deterministic equal widths and alignment.
  - Evidence: integration coverage checks every row width; updated table goldens cover left, center, and right aligned wrapped content.
- [x] Visible mapped atoms retain exact styles and UTF-8 source ranges.
  - Evidence: the repeated-text/style regression validates every displayed fragment against its original source slice; existing provenance tests pass.
- [x] Non-table wrapping remains unchanged.
  - Evidence: the implementation is private to `rendered/table.rs`; the complete rendered and source viewport suites pass.

## Main-thread review

Reviewed the mapped-fragment partitioning, exact-boundary behavior, consumed separator ownership, progress for over-wide fragments, table-width invariants, and snapshot changes. Corrected one provenance-test expectation so it distinguishes preserved interior whitespace from intentionally consumed wrap-boundary whitespace. No unrelated implementation scope was introduced.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

None
