# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 2

## Files changed

- `crates/oom-edit-core/src/rendered/nav.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/src/style.rs`
- `crates/oom-edit-core/tests/conformance/mod.rs`
- `crates/oom-edit-core/tests/public_api.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/screens/rendered.rs`

## Acceptance criteria

- [x] Wrapped-cell character selection paints only selected source fragments.
  - Evidence: an integration regression selects across continuation rows beside a populated cell and asserts every selected range remains within the intended cell.
- [x] Neighboring cells and logical rows remain unselected.
  - Evidence: source-range bounds exclude the populated neighboring cell and following row; a non-linear unit layout proves unrelated screen-adjacent atoms are omitted.
- [x] Forward/reverse and yank/delete/change share exact normalized ranges.
  - Evidence: endpoint swap preserves the same ranges, system yank matches their concatenated payload, and delete/change remove those ranges exactly once.
- [x] Multiple intervals on one visual row remain independent.
  - Evidence: the public DTO now stores `Vec<Range<usize>>`; unit and TUI tests assert two intervals remain separated by an unpainted synthetic table separator.
- [x] Resize, line, and block behavior remains stable.
  - Evidence: wrapped-table ranges survive a width rebuild; the complete conformance matrix for all three shapes passes.
- [x] UTF-8, synthetic provenance, API guards, and wrapper isolation remain valid.
  - Evidence: existing Unicode/entity and dependency-hygiene suites pass; the downstream public-API guard constructs the new DTO shape.

## Main-thread review

Reviewed source-range authority, row re-projection after resize, synthetic-endpoint compatibility, interval normalization, TUI composition, and the consumer-owned Vim translation. Preserved the established screen fallback only when an endpoint is synthetic. The first standard gate found Rust 1.97's singleton-range-vector Clippy lint; attempt 2 introduced an explicit singleton helper and equivalent lint-clean test assertions without changing behavior.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS (attempt 2) |

## Remaining concerns

None
