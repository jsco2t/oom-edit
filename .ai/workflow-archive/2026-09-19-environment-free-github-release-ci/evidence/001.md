# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `.github/workflows/ci.yml`
- `Makefile`
- `scripts/test_ci_workflow.py`
- `CONTRIBUTING.md`

## Acceptance criteria

- [x] `.github/workflows/` contains a valid, minimal workflow for every pull request and for pushes restricted to `main`.
  - Evidence: `ci.yml` declares unrestricted `pull_request` and a `push.branches` list containing only `main`; Ruby's YAML parser accepted the file.
- [x] The workflow uses read-only contents permission, a finite timeout, immutable action SHAs, exact audit-tool versions, no service infrastructure, and a single repository verification command: `make ci`.
  - Evidence: the workflow declares `contents: read`, a 30-minute timeout, two full-SHA action references, exact cargo-audit/cargo-deny versions with installer fallback disabled, no services, and one `run` entry.
- [x] `make ci` runs the existing release binary build before `make check` and returns nonzero on either failure.
  - Evidence: the target has two sequential recursive Make recipes, and `make ci` completed successfully.
- [x] A dependency-free contract test detects drift in all workflow invariants listed above.
  - Evidence: `scripts/test_ci_workflow.py` contains seven standard-library unit tests covering triggers, permissions, timeout, action/tool pins, infrastructure exclusion, Make ownership, ordering, registration, and documentation.
- [x] The contract test is included in the canonical test and check paths.
  - Evidence: `ci-workflow-test` is a prerequisite of `test` and its command is included in the summarized `check` test block.
- [x] The new Make target is discoverable through `make help` and documented in `CONTRIBUTING.md`.
  - Evidence: both `ci` and `ci-workflow-test` appear in `make help`; the developer target table documents `make ci`.
- [x] No Cargo manifest, lockfile, vendored source, or product implementation is changed.
  - Evidence: the scoped Git diff for `Cargo.toml`, `Cargo.lock`, `vendor`, and `crates` is empty.

## Main-thread review

Reviewed the actual workflow, Make orchestration, contract tests, and documentation against every task criterion. Confirmed release-build-before-check ordering, branch trigger scope, action and tool pinning, least privilege, lack of external application infrastructure, canonical test registration, and absence of product/dependency changes. No high-confidence defects remained.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make ci-workflow-test` | PASS |
| `make ci` | PASS |

The first sandboxed `make ci` attempt reached and passed build/tests but could not lock the user-level Cargo advisory databases. Rerunning outside that filesystem restriction passed; this was an execution-environment restriction, not a repository failure.

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make fmt-check` | PASS |
| `make check` | PASS |
| `make ci` | PASS |

## Remaining concerns

None
