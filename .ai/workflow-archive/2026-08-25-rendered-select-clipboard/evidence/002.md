# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/clipboard.rs`

## Acceptance criteria

- [x] All RFC 4648 remainder-length vectors, empty input, multiline input, and UTF-8 input produce canonical padded output.
  - Evidence: Existing six RFC vectors now assert canonical padding; empty, multiline, and UTF-8 cases assert their exact canonical strings.
- [x] A byte-exact test proves the full OSC 52 sequence including escape prefix, `c` selector, padded payload, and BEL terminator.
  - Evidence: `osc52_sequence_format` compares captured bytes directly with `\x1b]52;c;dGVzdA==\x07`, independent of the encoder under test.
- [x] A raw payload exactly at 100 KiB succeeds; maximum plus one returns `ClipboardError::TooLarge` and emits no bytes.
  - Evidence: The maximum test asserts prefix, suffix, and exact encoded length after success; the oversize test asserts the typed error and an empty capture.
- [x] Injected write and flush failures return `ClipboardError` and do not panic.
  - Evidence: Separate `WriteFailWriter` and `FlushFailWriter` tests assert exact project-owned errors; the flush case also proves the sequence was written before flush failed.
- [x] No dependency, alternate backend, terminal probe, or clipboard-read path is added.
  - Evidence: The cumulative source diff changes only the in-tree encoder, comments, and unit tests in the existing TUI clipboard module; manifests and boundary APIs are untouched.

## Main-thread review

Reviewed canonical group handling, raw-versus-encoded limit placement, full-sequence bytes, error propagation, and scope. Corrected stale comments that still described unpadded output and a nonspecific 100 KB limit. No high-confidence defect remained after review.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
