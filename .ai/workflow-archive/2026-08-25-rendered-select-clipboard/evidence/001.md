# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/vim.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit-core/tests/conformance/mod.rs`

## Acceptance criteria

- [x] Plain projected character, line, and block yanks each emit exactly one clipboard effect with the exact payload.
  - Evidence: Private wrapper tests assert a one-element clipboard payload list for all three projected shapes; session and conformance tests drive plain `y` for each public Select shape.
- [x] Plain yanks still populate the unnamed and `"0` slots and mirror the complete slot into `+`/`*`; both ordinary and explicit-system puts preserve character, line, and block shape.
  - Evidence: Wrapper tests compare text, linewise, blockwise, and block-width fields across the unnamed, `"0`, and system slots. The conformance shape matrix performs and undoes both ordinary `P` and `"+P` for every shape.
- [x] Explicit `"+y` and `"*y` emit exactly once, while named and black-hole yanks do not implicitly emit or replace the mirrored system slot.
  - Evidence: Session/conformance loops assert a single exact write for both explicit aliases; the wrapper exclusion test asserts no named/black-hole effects and an unchanged system slot.
- [x] Plain rendered delete/change operations and source Normal-mode yanks do not gain implicit clipboard output; existing explicitly system-targeted semantics remain intact.
  - Evidence: The projected operator matrix asserts no clipboard effects for unnamed delete/change. Existing private Normal-mode non-emission tests remain passing, while conformance continues to exercise explicit system-targeted delete/change effects.
- [x] Empty and synthetic-only selections remain non-mutating and do not emit a projected clipboard effect.
  - Evidence: The existing projected empty-selection operator matrix and public empty-selection conformance cases remain passing; copyable synthetic link-index behavior remains on its distinct typed-effect path.
- [x] Session and conformance tests drive plain `v…y`, `V…y`, and `Ctrl-V…y`, prove unchanged text and Normal-mode return, and cover exact source provenance across existing UTF-8, entity, Markdown, wrapping, table, repeated-text, and synthetic-gap cases.
  - Evidence: The updated session tests cover Unicode combining/CJK text, entities, wrapped tables, table separator gaps, and source-backed link-index spans. Conformance covers Markdown constructs, all selection shapes, block gaps, reflow, and exact output cardinality.
- [x] The conformance manifest explicitly guards the default rendered Select clipboard behavior.
  - Evidence: `SEL-6:default-system-clipboard` is required and mapped exactly once alongside the retained explicit `SEL-6:system-clipboard` row.

## Main-thread review

Reviewed the implementation against every task criterion and the frozen architecture. The default operation remains an unnamed yank, the mirror copies the complete canonical engine slot only after recording, explicit system yanks are not double-emitted, and no third-party type or new boundary escaped `vim.rs`. No high-confidence defect remained after review.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
