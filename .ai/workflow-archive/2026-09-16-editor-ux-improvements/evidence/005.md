# Evidence — Task 005

## Implementation

- Implemented by: main
- Attempt count: 2

## Files changed

- `crates/oom-edit/src/command/registry.rs`
- `crates/oom-edit/src/command/keymap.rs`
- `crates/oom-edit/src/widgets/hint_bar.rs`
- `crates/oom-edit/src/widgets/which_key.rs`
- `crates/oom-edit/src/overlay/palette.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/snapshot_tests.rs`
- `crates/oom-edit/tests/snapshots/`
- `patches/hjkl-engine/`, `Cargo.toml`, `Cargo.lock`, `vendor/`, `docs/dependencies.md`, `patches/README.md`
- `crates/oom-edit-core/tests/session_integration.rs` and the saved conformance regression seed

## Acceptance criteria

- [x] Bare `?` opens the palette only in Normal when no search prompt owns input.
- [x] Space+`?` and Space+`h` open the same palette in Normal and Select, including terminals that report Shift for `?`.
- [x] `/` search remains available; active search prompt and overlay input remain exclusive. Ctrl-modified `?` does not dispatch the palette alias.
- [x] Registry aliases project into quick hints, which-key continuations, and one contextual palette command row. Binding uniqueness and projection tests cover both aliases.
- [x] Snapshots show the new key hints and palette binding without duplicate command rows.

## Main-thread review

The snapshot suite exposed a saved randomized conformance case where the pinned `hjkl-engine` helper split a multibyte Unicode line ending during Insert exit. The fix is maintained through the repository's local patch mechanism, with provenance and removal criteria documented. `make vendor` regenerated `Cargo.lock` and `vendor/` offline; no package or version was added. The focused NEL Insert-exit regression and saved property case pass. The reviewed local fork delta is limited to two rope row helpers and their tests. `git diff --check` and a direct rustfmt check of the patch pass.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test-update-snapshots` | PASS |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS (fmt-check, lint, build, full test, deny, audit, data-license-check) |

The first gate found one unused palette import. It was removed, and task validation plus the full gate passed on attempt 2.

## Remaining concerns

None
