# Evidence — Task 006

## Implementation

- Implemented by: main
- Attempt count: 2 (the first `make check` could not lock the advisory cache in the sandbox; it passed with cache access)

## Files changed

- `crates/oom-edit/src/gutter.rs`
- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/app.rs`

No snapshot contained the gutter dot, so no golden file changed in this round.

## Acceptance criteria

- [x] The `•` marker occupies one cell and retains warning/error theme roles and the non-color error modifier.
  - Evidence: glyph-width and exhaustive role/modifier tests in `gutter.rs`; all-tier cell-style test in `screens/editor.rs`. The smaller glyph is the agreed visual-spacing choice; exact ink margins remain terminal-font dependent.
- [x] Three-digit rows keep a five-cell gutter and a blank separator after the last digit.
  - Evidence: exact `•148 ` and ` 148 ` cell assertions, with width five, in `screens/editor.rs`; `GUTTER_CONTENT_GAP` remains one in `widgets/status_bar.rs`.
- [x] Relative labels and one-, two-, and four-digit widths remain correct; marker presence does not move the body start.
  - Evidence: existing width-boundary tests plus updated exact marker/relative-row assertions. Both marked and unmarked rendered rows retain the same body start.
- [x] Source/rendered seams, narrow windows, cursor placement, and pointer hit testing retain their geometry.
  - Evidence: source and rendered cell tests assert separator and first body character, narrow gutter tests pass, and the shared `gutter_width` and App pointer translation are unchanged. Full test suite includes cursor and pointer regressions.
- [x] Exact-cell tests and the quality gate pass.
  - Evidence: `make test` and `make check` passed. Existing snapshots passed without updates.

## Main-thread review

Reviewed the marker mapping, cell composition, source/rendered seams, and unchanged width/pointer calculations. `git diff --check` passed. The only task implementation change is the TUI marker glyph and matching regression assertions. The first gate failure was isolated to advisory-cache lock permissions; no source repair was needed.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS (including repeat after the sandboxed gate failure) |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS (fmt-check, lint, build, full test, deny, audit, data-license-check) |

## Remaining concerns

Actual side bearing of `•` varies by terminal font; the requested pixel margin cannot be controlled by the text-cell renderer.
