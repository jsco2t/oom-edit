# Evidence — Task 004

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/overlay/mod.rs`
- `crates/oom-edit-core/src/rendered/nav.rs`
- `crates/oom-edit-core/src/session.rs`

## Acceptance criteria

- [x] Rendered clicks after long wheel scrolling move the cursor to the visible source-backed location without snapping back.
- [x] Insert clicks use source viewport geometry, including horizontal windows; source drags work across wrapped rows and Unicode-safe offsets.
- [x] First drag enters character Select, repeated drags update the selection, and release preserves it. Batched source drags retain the source frame geometry until the next paint.
- [x] Wheel scrolling moves viewport and cursor together in rendered and source modes, stops at boundaries, and retains the viewed content across mode changes.
- [x] Body, gutter, status, and modal boundaries are respected; overlays block all document mouse input.

## Main-thread review

Reviewed pointer geometry against both screen renderers, source wrap behavior, and gesture transitions. `git diff --check` passed.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS (fmt-check, lint, build, full test, deny, audit, data-license-check) |

## Remaining concerns

None
