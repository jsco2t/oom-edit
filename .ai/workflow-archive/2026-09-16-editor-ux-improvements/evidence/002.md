# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 2

## Files changed

- `crates/oom-edit/src/widgets/status_bar.rs`
- `crates/oom-edit/src/widgets/which_key.rs`
- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/event.rs`
- `crates/oom-edit/tests/snapshots/`

## Acceptance criteria

- [x] Unindented document text begins exactly one cell after the final gutter digit, with and without markers.
  - Evidence: compact marked/unmarked gutter cell tests and source/rendered body seam tests.
- [x] Short documents use a narrower gutter; signed relative and four-digit labels remain intact.
  - Evidence: `gutter_width` cases cover 0, 9, 10, 99, 100, 218, and 1000 lines with absolute/relative modes; existing relative marker tests still pass.
- [x] The rightmost status cell is blank in normal, prompt, and hint presentations, including narrow windows.
  - Evidence: new exact-cell status and which-key tests at widths 1 through 60; prompt cursor remains inside the inset.
- [x] Rendered/source snapshots show intended shifts only.
  - Evidence: `make test-update-snapshots` completed and the updated source, rendered, relative-number, and which-key snapshots were reviewed. Changes are the narrower gutter, reflow from extra text width, and one-cell status inset.

## Main-thread review

Reviewed geometry changes and snapshots; `git diff --check` passed. The first sandboxed `make check` passed format, lint, build, tests, and data licenses but could not create advisory database locks under `~/.cargo`. No product fix was needed. Task validation was repeated, then the full gate passed with the required advisory database access.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS (fmt-check, lint, build, full test, deny, audit, data-license-check) |

## Remaining concerns

None
