# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 2

## Files changed

- `crates/oom-edit-core/src/rendered/blocks.rs`
- `crates/oom-edit-core/src/rendered/mod.rs`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit-core/tests/goldens/kitchen_sink.txt`
- `crates/oom-edit-core/tests/goldens/vw8_blockquotes.txt`
- `crates/oom-edit/tests/snapshots/confirm_quit.txt`
- `docs/markdown-spec.md`

## Acceptance criteria

- [x] Consecutive prose source lines render as distinct, correctly numbered rows.
  - Evidence: Wide-layout regression reproduces the supplied shape and asserts four distinct source ranges and numbers.
- [x] Soft breaks and both hard-break syntaxes preserve row boundaries without changing source bytes.
  - Evidence: Focused soft/hard-break tests cover plain, two-space, and backslash endings and assert that break syntax creates no visible atom.
- [x] Long physical lines wrap independently with repeated ranges and unnumbered continuations.
  - Evidence: Narrow-layout regression asserts repeated physical-line ranges for continuations and a distinct numbered next source line.
- [x] Cursor, status identity, resize, navigation, search, and Select track physical lines.
  - Evidence: Session regressions exercise vertical/horizontal movement, canonical cursor rows, resize, search, and selection endpoints.
- [x] Character, line, and block selections/operators retain exact source ranges across breaks.
  - Evidence: Integration coverage asserts exact ranges and clipboard payloads for all three selection shapes.
- [x] Nested styles, links, lists, quotes, Unicode, LF, and CRLF retain byte-exact provenance.
  - Evidence: Focused layout tests cover nested emphasis, links spanning breaks, generated markers/prefixes, block quotes, lists, Unicode, and CRLF.
- [x] Code-span line endings remain normalized within one rendered row.
  - Evidence: Explicit code-span regression asserts `alpha beta` remains one numbered row.

## Main-thread review

The first complete gate identified one TUI confirmation snapshot that also contained the affected prose. That expected golden was updated, the full snapshot-update suite passed, and both task and standard gates then passed. Review also removed the old flattening path from headings so all non-table inline composition carries break identity consistently; table cells retain their specified single-line behavior.

## Task-specific validation

| Command | Result |
| --- | --- |
| `cargo test -p oom-edit-core --offline --locked soft_break` | PASS |
| `cargo test -p oom-edit-core --offline --locked hard_break` | PASS |
| `cargo test -p oom-edit-core --offline --locked rendered_line_number` | PASS |
| `cargo test -p oom-edit-core --offline --locked rendered_navigation` | PASS |
| `cargo test -p oom-edit-core --offline --locked rendered_select` | PASS |
| `cargo test -p oom-edit-core --offline --locked random_mode_width_and_motion_mapping_stays_in_bounds` | PASS |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
