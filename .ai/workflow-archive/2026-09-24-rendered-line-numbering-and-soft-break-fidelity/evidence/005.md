# Evidence — Task 005

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/theme.rs`
- `crates/oom-edit/src/screens/editor.rs`

## Acceptance criteria

- [x] Default-dark TrueColor displays `↳` with effective foreground distinct
      from the gutter background.
  - Evidence: The composed-buffer regression first failed with both colors at
    `Rgb(47, 52, 62)` and now passes with the continuation derived from a
    visible gutter role.
- [x] Colored built-in and test-loaded custom theme tiers cannot assign the
      continuation marker the gutter background color.
  - Evidence: Theme contracts cover every built-in tier and the lowered custom
    theme; legacy palettes fall back to the current-line gutter foreground only
    when the ordinary gutter foreground equals the surface.
- [x] The marker remains dim/italic and distinct from ordinary numbers, while
      monochrome and accessible tiers remain color-free.
  - Evidence: Existing modifier/accessibility assertions were retained and
    expanded alongside the new contrast invariant.
- [x] A diagnostic glyph and `↳` render together in their established cells
      without changing gutter width or content alignment.
  - Evidence: The composed gutter test now runs every built-in theme and tier,
    asserting `•  ↳ `, the marker cell style, and the trailing content gap.
- [x] Focused gutter/theme suites and the complete quality gate pass without
      changes outside the approved TUI presentation scope.
  - Evidence: Only the TUI theme implementation and its direct gutter test
    changed; every validation command below passed.

## Main-thread review

The initial test-forward run reproduced the screenshot exactly: default-dark
TrueColor resolved the continuation foreground and gutter background to the
same RGB value. Reusing ordinary gutter text fixed that case, while the expanded
matrix revealed the same collision in legacy default-light Color16. The theme
builder now uses ordinary gutter text when visible and falls back to the
existing current-line gutter foreground only when the ordinary color equals the
surface. Review also added the initially missing custom-theme lowering check.

## Task-specific validation

| Command | Result |
| --- | --- |
| `cargo test -p oom-edit --offline --locked gutter_continuation` | PASS |
| `cargo test -p oom-edit --offline --locked rendered_gutter` | PASS |
| `cargo test -p oom-edit --offline --locked theme::tests` | PASS |
| `cargo test -p oom-edit --offline --locked snapshot_tests` | PASS |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
