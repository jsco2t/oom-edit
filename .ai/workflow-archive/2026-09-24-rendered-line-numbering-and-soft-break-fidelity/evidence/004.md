# Evidence — Task 004

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `examples/kitchen-sink.md`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`

## Acceptance criteria

- [x] The example visibly and accurately demonstrates every recent static Markdown scenario listed in scope.
  - Evidence: The corpus now includes physical versus wrapped rows, numbered physical blanks, both hard-break forms, nested soft breaks, a multiline code span, loose lists, the list/blank/heading boundary, and separated spell diagnostics.
- [x] Every top-level thematic-rule-delimited section has exactly one concise, consistently formatted expected-rendering callout.
  - Evidence: Twenty-two `> **Expected rendering:** ...` callouts cover the opening demonstration and all 21 named top-level sections; the contract asserts their exact inventory and rendered quote form.
- [x] The mislabeled or absent Markdown variants are corrected.
  - Evidence: The corpus now contains correctly labeled `*`, `+`, and `-` lists, `---`/`***`/`___` rules, actual Setext underlines, an ordered `)` list, and a tilde fence.
- [x] Existing front matter, long lines, nested structures, language fences, and Go tab bytes remain present.
  - Evidence: The contract asserts the YAML boundary and literal Go tabs; existing kitchen-sink and source-viewport tests pass unchanged.
- [x] A focused test reads the real example and verifies source and rendered behavior.
  - Evidence: `example_kitchen_sink_recent_regressions_are_explicit_and_renderable` checks raw syntax, callouts, wide physical rows, narrow continuations, numbered blanks, loose-list separation, quote lines, and code-span normalization.
- [x] Existing kitchen-sink and neighboring Markdown/rendering tests pass.
  - Evidence: Every focused validation command below passed.
- [x] The full gate passes without product, dependency, public-API, or build-workflow changes.
  - Evidence: Only the example corpus and its test changed in round 2; `make check` passed all seven stages.

## Main-thread review

The first focused run exposed a test-only assumption that a loose-list blank
would remain `Synthetic`; the current physical-blank contract correctly makes
it an atom-free numbered `Content` row, so the corpus assertion was aligned
with the established behavior. Review also corrected the ordered-list callout
to distinguish accepted `)` source syntax from the renderer's standardized
visible numbering and expanded the opening callout to cover existing YAML and
`Space m` behavior. The literal two-space hard break is intentional trailing
whitespace and is protected by an exact-byte assertion.

## Task-specific validation

| Command | Result |
| --- | --- |
| `cargo test -p oom-edit-core --offline --locked example_kitchen_sink_recent_regressions` | PASS |
| `cargo test -p oom-edit-core --offline --locked kitchen_sink` | PASS |
| `cargo test -p oom-edit-core --offline --locked soft_break` | PASS |
| `cargo test -p oom-edit-core --offline --locked hard_break` | PASS |
| `cargo test -p oom-edit-core --offline --locked loose_list` | PASS |
| `cargo test -p oom-edit-core --offline --locked setext` | PASS |
| `cargo test -p oom-edit-core --offline --locked thematic_break` | PASS |
| `cargo test -p oom-edit-core --offline --locked --test source_viewport` | PASS |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
