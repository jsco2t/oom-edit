# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/rendered/mod.rs`
- `crates/oom-edit-core/src/rendered/nav.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/screens/rendered.rs`

## Acceptance criteria

- [x] A list item followed by a physical blank line and heading renders the blank as a numbered content row.
  - Evidence: Core and TUI regressions reproduce the list/container boundary and assert the blank row's source line number.
- [x] Remapping onto the row yields the blank line's canonical source position and status ruler value.
  - Evidence: The session regression enters the blank row and asserts canonical line/column identity; the TUI regression asserts the displayed ruler.
- [x] The row owns the exact LF/CRLF blank-line range and has no display atoms.
  - Evidence: Unit coverage exercises LF, CRLF, and Unicode-adjacent input; the session regression asserts an empty styled row and atom list.
- [x] Absolute and relative gutters show the correct value for the row.
  - Evidence: TUI regression renders both line-number modes and checks the gutter cell value.
- [x] Adjacent blocks remain separated synthetically, while multiple blanks normalize to one displayed row.
  - Evidence: Core regression covers adjacent blocks and multiple source blanks without changing the single-separator policy.
- [x] Existing blank-line, loose-list, paragraph-boundary, and synthetic-row behavior remains passing.
  - Evidence: Focused suites and the full repository gate passed.

## Main-thread review

The initial physical-line lookup fixed the missing row but exposed an overlapping-container remap: the enclosing list span could win before the exact blank row. The fallback selection now prefers an exact source start and then the narrowest containing range, preserving canonical blank-line identity without a TUI workaround. No remaining high-confidence defects were found.

## Task-specific validation

| Command | Result |
| --- | --- |
| `cargo test -p oom-edit-core --offline --locked rendered_blank_line` | PASS |
| `cargo test -p oom-edit-core --offline --locked loose_list` | PASS |
| `cargo test -p oom-edit-core --offline --locked rendered_line_number` | PASS |
| `cargo test -p oom-edit --offline --locked rendered_blank_line` | PASS |
| `cargo test -p oom-edit --offline --locked rendered_gutter` | PASS |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
