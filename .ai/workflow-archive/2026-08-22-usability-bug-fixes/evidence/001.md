# Evidence — Task 001

## Implementation

- Implemented by: worker
- Attempt count: 2

## Files changed

- `crates/oom-edit/src/command/registry.rs`
- `crates/oom-edit/src/widgets/hint_bar.rs`
- `crates/oom-edit/src/overlay/palette.rs`
- `crates/oom-edit/src/theme.rs`
- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/src/snapshot_tests.rs`
- `crates/oom-edit/tests/snapshots/*.txt` (affected palette, hint-bar, and editor snapshots)
- `crates/oom-edit/tests/dependency_hygiene.rs`
- `crates/oom-edit-core/tests/perf_smoke.rs`

## Acceptance criteria

- [x] Normal quick help has registry-owned `v=select`, `/=search`, and `:=command` units plus one `Space [h=help, w=save, q=quit]` group; the ambiguous slash wording was removed.
- [x] Hint formatting measures Ratatui display width and boundary tests prove that complete standalone and grouped units are retained or dropped without slicing.
- [x] Executable and reference palette rows use one pure column calculation; tests assert exact row width and equal description/tail offsets at 40 and 80 cells.
- [x] Palette surface, text, secondary, and selected slots are explicitly defined exactly once in every built-in theme tier; glyph markers and modifiers distinguish reference, disabled, and selected states without color alone.
- [x] Existing filtering, navigation, execution, reference non-executability, viewport, registry, and theme completeness tests pass.
- [x] Wide and 40x12 palette snapshots and all affected hint/editor snapshots were updated and pass deterministic snapshot checks.

## Review

Main review corrected the palette separator width budget, required explicit theme-table entries instead of fallback-only styling, removed duplicate slot entries, added exact column-offset and theme-slot uniqueness assertions, shortened quick labels to the report's intended wording, and retained the grouped Space hint at its registry-priority position. Gate failures also exposed two pre-existing harness defects: nested `make` output was made deterministic with `--no-print-directory`, and timing-sensitive performance smoke tests were serialized so their budgets are not distorted by mutual contention.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make fmt-check` | PASS |
| `make lint` | PASS |
| `make build` | PASS |
| `make test` | PASS |
| `make check` | PASS |

## Remaining concerns

None.
