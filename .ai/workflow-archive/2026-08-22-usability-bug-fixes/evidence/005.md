# Evidence — Task 005

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `README.md`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/config.rs`
- `crates/oom-edit/src/event.rs`
- `crates/oom-edit/src/lib.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/terminal_guard.rs`

## Acceptance criteria

- [x] Missing and enabled configuration maps Normal/Insert/Select/Command to steady block/bar/underscore/bar; configuration tests cover defaults, a partial editor section, explicit false, and serialization round trips.
- [x] Disabled cursor shapes map all four public modes to a steady block.
- [x] Existing Insert and Command cursor-position tests remain green, and a new rendered test verifies visible Normal and Select cursor coordinates after horizontal cropping.
- [x] The rendered cursor-position test also verifies that Normal's painted cursor cell and Select's reversed selection carrier remain present.
- [x] Event-boundary tests verify exact Crossterm escape output and suppress repeated output for unchanged mapped shapes, including Insert-to-Command transitions.
- [x] Ordinary, panic, and error cleanup share the user-default cursor reset; the Unix fatal-signal literal includes the same reset before mouse and alternate-screen cleanup.
- [x] README documents `editor.cursor_shapes = true` and the exact current-mode defaults.
- [x] Existing mode and screen snapshots passed unchanged, confirming the new terminal cursor does not replace or alter semantic rendered output.

## Review

The main-thread review confirmed that only the project-owned `Mode` crosses from App into the terminal boundary and that Crossterm cursor types remain TUI-private. The first rendered-position test expectation assumed a four-cell gutter; it was corrected to use the canonical gutter-width calculation, matching production geometry. No implementation defect remained after review.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make fmt-check` | PASS |
| `make lint` | PASS |
| `make build` | PASS |
| `make test` | PASS |
| `make check` | PASS |

## Remaining concerns

None.
