# Evidence — Task 003

## Implementation

- Implemented by: worker
- Attempt count: 2

## Files changed

- `crates/oom-edit-core/src/rendered/mod.rs`
- `crates/oom-edit-core/src/rendered/table.rs`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`
- `crates/oom-edit-core/tests/fixtures/vw9_tables.md`
- `crates/oom-edit-core/tests/goldens/vw9_tables.txt`

## Acceptance criteria

- [x] Over-budget tables render at no more than 80 display cells when supplied widths from 40 through 80; allocator and golden coverage establish the floor behavior.
- [x] Wider surfaces use their supplied budget without exceeding the existing 40-cell per-column cap; deterministic allocation is asserted at widths 81 and 120.
- [x] Narrowed cells wrap without loss and all border, separator, and data rows retain identical Unicode display width.
- [x] Left, center, and right alignment continues through allocated-width wrapping; the responsive fixture and existing alignment assertions cover first and continuation rows.
- [x] CJK, combining marks, entities, escapes, inline code, repeated text, and links retain byte-exact source provenance and semantic styling across 40, 79, 80, 81, and 120-cell layouts.
- [x] Only table layout receives the 80-cell floor; other rendered blocks continue using the actual renderer width.

## Review

Main-thread review found that empty natural columns could remain zero-width, allocator boundary behavior lacked direct coverage, and the existing golden did not exercise a truly over-budget table. Empty columns now retain one content cell; focused tests cover floor, wider-width, deterministic tie-breaking, non-inflation, and structural overflow; and the VW-9 fixture/golden now contains a three-column table that naturally exceeds 120 cells. The first standard-gate attempt found one rustfmt-only mismatch, which was corrected before the full gate was restarted.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make fmt-check` | PASS |
| `make lint` | PASS |
| `make build` | PASS |
| `make test` | PASS |
| `make check` | PASS |

## Remaining concerns

None.
