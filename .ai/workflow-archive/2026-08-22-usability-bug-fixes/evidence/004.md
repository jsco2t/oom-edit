# Evidence — Task 004

## Implementation

- Implemented by: main
- Attempt count: 3

## Files changed

- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/snapshot_tests.rs`
- `crates/oom-edit/tests/snapshots/rendered_table.txt`

## Acceptance criteria

- [x] Rendered right/left movement updates a distinct horizontal viewport with the existing scroll margin and keeps the active cursor inside the visible text surface.
- [x] Display-cell cropping applies only to the text area; buffer comparison proves the gutter and line-number cells remain fixed.
- [x] Cropping occurs after semantic, diagnostic, search, selection, cursor-row, and Normal-cursor composition, and tests prove every resulting style survives unchanged.
- [x] Narrow-to-wide resize clamps the offset to zero while preserving the canonical source cursor.
- [x] Each `TabEntry` retains an independent rendered offset, including after switching back and refollowing.
- [x] Short prose resets an unnecessary rendered offset, while the existing source Insert wrap/nowrap and horizontal-scroll suites remain green.
- [x] CJK cursor atoms are followed using their full two-cell width; crop-boundary tests keep wide glyphs whole and preserve combining sequences.

## Review

Main-thread review added direct CJK cursor-width coverage, composited-style preservation coverage, lifecycle reset assertions, and a narrow horizontally-scrolled table snapshot. The first gate attempt found rustfmt-only differences. The second found that the expanded screen adapter exceeded Clippy's argument limit; rendered top/left coordinates were consolidated into the small `RenderedViewport` presentation DTO, matching the repository's existing viewport pattern without suppressing lint.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make fmt-check` | PASS |
| `make lint` | PASS |
| `make build` | PASS |
| `make test` | PASS |
| `make check` | PASS |

## Remaining concerns

None.
