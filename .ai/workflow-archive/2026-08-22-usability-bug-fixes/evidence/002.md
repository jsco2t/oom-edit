# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/rendered/mod.rs`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`

## Acceptance criteria

- [x] Every generated link-index row receives `TargetKind::Link(index)` at layout construction, and tests verify that all of its atoms remain source-less.
- [x] Plain `y` and Enter in rendered Normal emit exactly one `ClipboardWrite` containing only the focused destination.
- [x] In rendered Select, `y` emits the destination and returns to Normal, while Enter emits it and remains in Select; the synthetic selection has no source ranges.
- [x] Repeated links and a Unicode destination are covered by index-driven tests without rendered-text lookup in production code.
- [x] The App routes the effect through the injected sink, reports `copied to system clipboard` on success, preserves clipboard errors on failure, and never mutates the document.
- [x] Existing inline-link Enter behavior, ordinary selection/register operators, navigation, public API guards, and the complete regression suite pass.

## Review

Main review ensured the new path is gated by both `LineKind::Synthetic` and the typed target, clears pending count/register state after a copy, and updates the existing nested-target count only for the intentionally added index-row target. No terminal type entered the core and no public API variant was added.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make fmt-check` | PASS |
| `make lint` | PASS |
| `make build` | PASS |
| `make test` | PASS |
| `make check` | PASS |

## Remaining concerns

None.
