# Evidence — Task 006

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `CHANGELOG.md`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/event.rs`
- `crates/oom-edit/src/terminal_guard.rs`

## Acceptance criteria

- [x] The redraw scheduler starts with exactly one pending frame, consumes it once, and records no further paints across repeated unchanged idle polls.
- [x] A bounded scripted event source proves three consecutive resizes are sampled individually, dispatched once at the final dimensions, followed by exactly one requested paint.
- [x] Key, paste, mouse, overlay, resize, which-key, transient-expiry, and spell-progress paths all retain their routing and request redraws when visible state may change.
- [x] Non-resize batch events retain order and individual post-read timestamps; a separate test proves a sustained ready source is capped at 32 events before presentation resumes.
- [x] Recording-sink tests prove successful and failed paints are enclosed by one begin/end synchronized-update pair and that the end command is attempted after a forced draw error.
- [x] Terminal setup/restore now centrally balances bracketed paste and existing modes; ordinary, panic, and Unix signal cleanup defensively end synchronization, reset cursor shape, disable paste/mouse modes, and leave the alternate screen before raw-mode restoration as applicable.
- [x] Existing event budget, spell interruptibility, resize remap, terminal guard, modal routing, snapshots, and complete workspace suites pass.
- [x] The Unreleased changelog covers command discovery, copyable link rows, responsive and horizontally navigable tables, cursor configuration, and stable synchronized frames.

## Review

Main-thread review added an explicit 32-event starvation bound, stopped a batch immediately after quit, strengthened the resize test to count one subsequent paint, and corrected stale continuous-redraw documentation. No unresolved scope or architecture issues remain.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |
| `make build-release` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make fmt-check` | PASS |
| `make lint` | PASS |
| `make build` | PASS |
| `make test` | PASS |
| `make check` | PASS |

## Remaining concerns

None.
