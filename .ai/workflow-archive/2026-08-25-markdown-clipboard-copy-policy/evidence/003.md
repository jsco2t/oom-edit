# Evidence — Task 003

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/rendered/blocks.rs`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`

## Acceptance criteria

- [x] The exact kitchen-sink escaped-backtick fragment has byte-exact rendered
      character-copy coverage.
  - Evidence: the integration test asserts both complete two-backtick delimiter
    runs and adjacent spaces in Markdown, plus the rendered literal-backtick
    payload in plain text.
- [x] Parser-leaf tests exclude outer delimiters for one-, two-, and
      longer-backtick spans.
  - Evidence: table-driven block-model coverage includes leading, trailing,
    repeated literal backticks, Unicode, and repeated text with exact source
    ownership assertions.
- [x] Normalization and provenance remain correct.
  - Evidence: LF, CRLF, table escaped-pipe, Unicode, and monotonic source tests
    pass; CRLF's two rendered spaces both own the exact CRLF byte range.
- [x] Partial, line, and block selection behaviors are covered.
  - Evidence: partial Markdown remains bounded to the selected inner content,
    linewise copy remains byte-exact, and a one-row block copy preserves both
    clipboard representations.
- [x] Both App copy-format paths emit the correct edge-case representation.
  - Evidence: the injected sink receives exact Markdown by default and the
    outer-delimiter-free rendered payload for `plain-text`.
- [x] Existing editing and clipboard behavior remains green.
  - Evidence: the full workspace test suite and quality gate pass.
- [x] No dependency or public API change was introduced.
  - Evidence: only private provenance implementation and existing tests changed;
    dependency and public API guards pass.

## Main-thread review

The fix stays at parser-leaf construction and does not change selection or Vim
operation DTOs. Review added explicit block-selection coverage. The initial task
test exposed incomplete CRLF mapping; the mapper now assigns both parser-rendered
spaces to the exact CRLF token rather than falling back to the whole code span.
No further high-confidence findings remain.

## Task-specific validation

| Command     | Result |
| ----------- | ------ |
| `make test` | PASS   |

## Standard quality gate

| Command      | Result |
| ------------ | ------ |
| `make check` | PASS   |

## Remaining concerns

None
