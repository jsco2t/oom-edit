# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/clipboard.rs`
- `crates/oom-edit-core/src/lib.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/tests/conformance/mod.rs`
- `crates/oom-edit-core/tests/public_api.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`

## Acceptance criteria

- [x] The exact user example emits every source backtick in the Markdown field
      and the exact syntax-free sentence in the plain-text field.
  - Evidence: `rendered_copy_preserves_markdown_and_prepares_sanitized_plain_text`
    drives the complete rendered selection and asserts both hardcoded forms.
- [x] Complete code, emphasis, strong, strikethrough, link, image, nested,
      escaped, and entity cases have byte-exact Markdown and plain-text tests.
  - Evidence: the conformance construct matrix and entity integration matrix
    assert both representations independently.
- [x] Partial selections prove no unselected syntax or content is added.
  - Evidence: `partial_inline_selection_does_not_add_unselected_delimiters`
    selects only `emph` inside emphasis and receives `emph` in both forms.
- [x] Character, line, and block coverage includes wrapping, tables, Unicode,
      repeated content, multiline output, and synthetic decorations.
  - Evidence: the selection-shape matrix now asserts both fields; existing
    source-provenance tests exercise wrapped tables, repeated source, UTF-8,
    empty block rows, and synthetic table boundaries through the new effect.
- [x] Link-index URL copies have equal fields and remain source-independent.
  - Evidence: link-index integration tests use `ClipboardContent::invariant`,
    and `invariant_content_has_equal_representations` asserts the DTO contract.
- [x] Plain/default and explicit system yanks retain their existing register
      contents and shapes; puts, delete, change, undo, empty selection, and source
      Normal-mode non-emission tests remain green.
  - Evidence: the unchanged Vim operation DTO remains the mutation input, and
    the complete conformance/Vim suites passed.
- [x] The public effect/DTO API is guarded by compile tests and exposes no
      third-party type.
  - Evidence: `ClipboardContent` is re-exported from the curated facade and
    named/constructed by `tests/public_api.rs`; dependency hygiene passed.
- [x] App integration compiles and emits the Markdown field as the default.
  - Evidence: `App::handle_effect` sends `content.markdown()` through the
    existing injected sink; the TUI unit suite and full build passed.

## Main-thread review

The review confirmed that edit/register ranges remain unchanged and clipboard
source expansion is copy-only. One high-confidence issue was found and fixed:
plain text for a block crossing a wrapped Markdown construct could have parsed an
unmatched per-row delimiter. Block plain text now comes directly from selected
source-backed rendered atoms, while block Markdown remains source-span based.

## Task-specific validation

| Command     | Result |
| ----------- | ------ |
| `make test` | PASS   |

## Standard quality gate

| Command      | Result |
| ------------ | ------ |
| `make check` | PASS — 7 passed, 0 failed |

## Remaining concerns

None
