# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 2

## Files changed

- `crates/oom-edit/src/config.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/event.rs`
- `crates/oom-edit/src/lib.rs`
- `README.md`
- `CHANGELOG.md`

## Acceptance criteria

- [x] Default and absent configuration send the Markdown representation to the
      sink, preserving the exact user example.
  - Evidence: `ClipboardCopyFormat::Markdown` is the serde/default variant;
    missing-section tests assert it, and
    `clipboard_copy_format_selects_exact_markdown_or_plain_text_once` asserts
    the exact backtick-preserving sentence through the injected App sink.
- [x] `copy_format = "plain-text"` sends only the sanitized representation for
      the same action.
  - Evidence: the same App test drives the same rendered selection and asserts
    one exact syntax-free write.
- [x] Config defaults, valid values, partial configs, save/load round trips, and
      invalid values have focused automated coverage.
  - Evidence: config tests cover missing sections/fields, both serialized enum
    values, default serialization, full save/load, and existing malformed-config
    fallback behavior.
- [x] App tests use the injected sink and prove exactly one write of the selected
      representation with unchanged status/error behavior.
  - Evidence: `SharedClipboardSink` captures exact calls; format selection,
    success feedback, and injected failures are asserted independently.
- [x] The 100 KiB acceptance/rejection boundary applies to selected bytes and no
      partial output is written on rejection.
  - Evidence: `clipboard_size_limit_applies_to_the_configured_representation`
    checks both reciprocal cases: Markdown oversized/plain accepted and Markdown
    accepted/plain oversized, with empty capture on each rejection.
- [x] URL-only copies behave identically in both modes.
  - Evidence: `format_invariant_link_copy_is_identical_for_both_preferences`
    asserts the same URL in both configured modes.
- [x] Internal register shape and `p`/`P` behavior are unchanged by the setting.
  - Evidence: `clipboard_copy_format_does_not_change_internal_put_behavior`
    drives identical rendered yank/put input under both preferences and asserts
    identical Markdown document results; the full Vim conformance suite passes.
- [x] README and changelog accurately describe the default, setting,
      sanitization, register scope, clipboard input, and OSC 52 limitations.
  - Evidence: README documents exact Markdown, plain-text conversion, outgoing
    scope, bracketed paste, selected-byte limit, and best-effort transport; the
    changelog records the setting and behavior change.

## Main-thread review

The review corrected ambiguous README size-limit wording and added the reciprocal
selected-representation boundary case. The first standard gate then found that
the new preference pushed `App::new_with_spell` over Clippy's argument limit.
Startup defaults were grouped into the owned `AppStartupOptions` boundary type,
keeping all configuration explicit without suppressing the lint.

## Task-specific validation

| Command     | Result |
| ----------- | ------ |
| `make test` | PASS   |

## Standard quality gate

| Command      | Result |
| ------------ | ------ |
| `make check` | PASS — 7 passed, 0 failed |

## Remaining concerns

None
