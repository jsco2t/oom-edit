# Task 002 Evidence: Theme Provenance and Canonical Notices

Status: complete

## Implementation

- Retained complete palette and license files from the exact immutable revisions pinned by the PRD for Catppuccin Mocha, Dracula, Nord, Solarized Dark, Tokyo Night Night/Storm, and the credited Enkia origin.
- Added a five-entry provenance manifest with repository, variant, revision, upstream path, complete-file SHA-256, retrieval date, SPDX, copyright/author facts, Tokyo Night's explicit copyright absence, and audit-asset paths.
- Added separate project-owned 18-role semantic mapping notes and a raw-asset SHA-256 manifest.
- Expanded `THIRD-PARTY-NOTICES.md` into the canonical source/binary notice surface while retaining the complete SCOWL section. Each bundled theme has exactly one section and its complete applicable license text; Tokyo Night includes both Apache-2.0 and Enkia's MIT notice.
- Changed `--licenses` to embed the root canonical notice file and extended its pre-terminal exact-output tests.
- Expanded the bundled-data compliance gate and mutation suite to hash-pin all reviewed surfaces and fail on missing/tampered raw assets, metadata, notices, or CLI embedding.

## Acceptance criteria

- [x] Every palette/license source and hash matches the immutable PRD table and has a clear provenance/mapping record.
  - Evidence: all 12 retained raw assets pass `MANIFEST.sha256`; focused Rust tests compare every complete-file digest.
- [x] `THIRD-PARTY-NOTICES.md` retains SCOWL and contains exactly one complete section for each bundled theme, including Tokyo Night's Apache and Enkia MIT obligations.
  - Evidence: the compliance script and integration test assert exact section counts and embed each complete raw license.
- [x] `oom-edit --licenses` emits the exact canonical notice file and exits before terminal setup.
  - Evidence: `license_notices_match_all_distribution_surfaces` compares binary stdout byte-for-byte with the root notice and requires empty stderr.
- [x] The compliance script fails on missing/tampered assets, provenance, notices, or CLI embedding and passes the unmodified repository.
  - Evidence: `data_license_check_fails_closed_for_contract_mutations` mutates every retained upstream asset and each authoritative surface; `make data-license-check` passes unmodified.
- [x] Gruvbox and Rosé Pine are absent, and no dependency or runtime network mechanism was added.
  - Evidence: compliance/Rust guards assert both names are absent; Cargo manifests, `Cargo.lock`, and `vendor/` are unchanged.

## Main-thread review

Verified all downloaded bytes against the PRD hashes before retaining them. Confirmed raw evidence remains distinct from project-owned mapping notes and the canonical notice, Tokyo Night records the required absence rather than an invented copyright, and the implementation adds no runtime data path or dependency.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make data-license-check` | PASS |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS — 7/7 gates |

## Remaining concerns

None.

