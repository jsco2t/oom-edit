# Evidence — Task 006

## Implementation

- Implemented by: main
- Attempt count: 2

## Files changed

- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/event.rs`
- `crates/oom-edit/src/gutter.rs`

## Acceptance criteria

- [x] New/open/replace/edit/reset/disable tests show immediate empty/invalidation state with zero diagnostic-to-line projections on synchronous paths.
  - Evidence: App tests cover fresh and opened tabs, replacement, paste-driven mutation, provider generation reset across tabs, and Space-z disable; every transition asserts empty completed/pending state and an unchanged projection counter.
- [x] Provider scan completes before bounded marker build, partial summaries remain invisible, final publication is atomic, and idle becomes quiescent.
  - Evidence: a 130-diagnostic test observes clean-publication detection at zero projections, two invisible 64-item units, a final two-item atomic publication, and a subsequent `on_idle_unit` returning false.
- [x] Input preemption stops before another marker unit and a canceled generation can never publish.
  - Evidence: the event-loop test injects pending terminal input after one 64-item marker unit and proves no second unit runs; the App cancellation test edits during a partial build and verifies the generation advances while completed and pending state remain empty.
- [x] Separate tabs preserve independent empty/pending/completed state; active rendering/work cannot project inactive tabs.
  - Evidence: a two-tab test completes the first snapshot, partially advances only the second after switching, verifies the first counter/state is unchanged, and restores the first completed snapshot on switching back.
- [x] Repeated App renders leave provider/build counters and snapshot memory unchanged.
  - Evidence: twenty `TestBackend` renders preserve the exact immutable snapshot, diagnostic projection count, and heap-capacity accounting.
- [x] Existing Trouble overlay and spell behavior remain correct, with no core/public/dependency change.
  - Evidence: the complete workspace test suite, dependency-hygiene tests, Trouble tests, spell-session tests, and public API guards pass without changes to core, manifests, lockfile, or vendor sources.

## Main-thread review

Review confirmed that publication captures only the diagnostic count and defers every offset-to-line projection to later 64-item idle units. Invalidation is constant-time per tab, provider resets clear all affected tabs without inspecting diagnostics, pending state remains tab-local, and `spell_tick` always gets the first opportunity to expose generation changes before marker projection. The first quality-gate attempt found a test-only fixed collection expressed as a `Vec`; it was replaced with an array before the complete validation and gate were rerun.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
