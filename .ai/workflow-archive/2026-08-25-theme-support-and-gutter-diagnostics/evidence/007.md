# Evidence — Task 007

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/gutter.rs`
- `crates/oom-edit/src/lib.rs`

## Acceptance criteria

- [x] Every gutter cell in both modes carries the selected gutter background for color themes, including blank/wrapped/synthetic/below-document cells and the separator gap.
  - Evidence: both document adapters fill the body surface and route through the single full-rectangle gutter painter; exact-cell source and rendered tests cover blank, wrapped, synthetic, and padding rows.
- [x] Ordinary/current line numbers and all four severity glyphs compose exact foreground/modifier/background styles at TrueColor, ANSI-16, and Monochrome.
  - Evidence: `gutter_composes_background_numbers_and_every_marker_at_all_tiers` checks symbols, foregrounds, backgrounds, and modifiers for every tier and severity.
- [x] Markers appear once on the numbered physical source row, never on continuations/synthetic rows, and remain fixed across rendered horizontal scroll and viewport changes.
  - Evidence: source-wrap and rendered-table tests inspect numbered versus synthetic rows at multiple horizontal offsets.
- [x] Relative numbers, signs/digits at 9/10/999/1000 boundaries, narrow/clipped areas, and active-line behavior remain geometrically correct.
  - Evidence: `gutter_preserves_relative_digit_sign_and_narrow_geometry` asserts exact rows at every requested digit boundary and at one- and two-cell widths.
- [x] A real published diagnostic path renders markers in source and rendered modes and an edit hides them before any rebuild.
  - Evidence: `published_marker_renders_in_both_modes_and_edit_hides_it_immediately` drives the App-owned asynchronous publication path through both public modes and a synchronous edit invalidation.
- [x] Render work/counters remain read-only and viewport-bounded; existing snapshots/tests remain correct except intentional focused marker layout updates.
  - Evidence: render receives only `DocumentPresentation` references, performs no diagnostic projection, and the complete suite and performance budget gates pass.

## Main-thread review

Reviewed the shared paint path, style patch order, narrow geometry, source-line ownership, and App integration. The review corrected one custom-theme test to inspect a body padding cell instead of the deliberately overlaid cursor-line cell. No product defect remained after review.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |
| `make bench-check` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
