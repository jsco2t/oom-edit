# Evidence — Task 004

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/theme.rs`
- `crates/oom-edit/src/lib.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/snapshot_tests.rs`

## Acceptance criteria

- [x] Complete dark/light fixtures parse and lower every slot/tier; all strict invalid-contract cases and exact size boundaries fail with stable categories.
  - Evidence: strict serde DTO tests cover all 18 RGB roles, optional closed ANSI overrides, missing/unknown fields, invalid colors/appearance/ANSI, unsupported include/inheritance keys, complete tier-row equality, and the exact 65,536/65,537-byte boundary with size-before-UTF-8 precedence.
- [x] Real filesystem tests prove direct-child filtering, lexical order, invalid sibling isolation, missing-directory silence, reserved names, invalid UTF-8, and startup-only immutability.
  - Evidence: temporary-directory tests cover direct regular `.toml` children, nested/non-TOML exclusion, sorted user origins, typed failures, silent absence, duplicate/reserved rejection, and reconstruction-only reload behavior.
- [x] Loader warnings are exactly one per rejected file, ordered, path-specific, and emitted before App/terminal construction.
  - Evidence: ordered `(path, kind)` assertions cover invalid name, reserved name, UTF-8, TOML, size, duplicate, and read failures; production emits the completed report before resolution, App construction, and terminal setup.
- [x] CLI, environment, dark/light config, matching fallback, cycle order, and persistence all use the loaded catalog and preserve unrelated/opposite config values.
  - Evidence: catalog resolution/cycle tests cover both appearances and every source; an App integration test cycles from `accessible` to a file-loaded theme and proves only the dark config slot changes.
- [x] A file-loaded theme reaches representative source/rendered/body/gutter/semantic/status cells through owned App/catalog lifetime.
  - Evidence: the real-file App test verifies custom body/gutter roles, semantic text in rendered Normal and source Insert, and the custom status surface after the catalog is moved into App.
- [x] Help/docs may be prepared for Task 008, but no unsupported feature or new dependency is introduced.
  - Evidence: schema rejects inheritance/includes and discovery is startup-only; no manifest, lockfile, or vendor changes exist.

## Main-thread review

Review confirmed bounded reads occur before UTF-8/TOML processing, direct-child discovery ignores symlinks/directories and sorts filenames, built-ins cannot be shadowed, invalid files remain isolated, and all selection sources enforce appearance compatibility. Legacy snapshot helpers were corrected to set the light display mode explicitly rather than bypass the new safety rule.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
