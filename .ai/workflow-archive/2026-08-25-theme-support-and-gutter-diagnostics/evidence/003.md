# Evidence — Task 003

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/theme.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/lib.rs`
- `crates/oom-edit/src/event.rs`
- `crates/oom-edit/src/snapshot_tests.rs`
- `scripts/check-data-licenses.sh`
- `crates/oom-edit/tests/dictionary_assets.rs`

## Acceptance criteria

- [x] One owned catalog drives built-in lookup, mode compatibility, fallback, cycling, and App rendering/persistence; old free routing paths are removed.
  - Evidence: production startup constructs one `ThemeCatalog`, resolves through it, moves it into `App`, and rendering/cycling query the App-owned instance. Static lookup compatibility helpers are test-only.
- [x] Registry names/order/modes are exact and unique, with five attributed entries and no Gruvbox/Rosé Pine.
  - Evidence: registry meta-tests assert the exact eight-entry order, fallback/mode rules, attribution pairs, exclusions, and catalog projections; the data-license gate cross-checks registry attribution against pinned provenance.
- [x] All built-ins resolve every closed semantic/UI slot at TrueColor, ANSI-16, and Monochrome and retain required modifiers/carriers.
  - Evidence: exhaustive catalog/tier tests cover all semantic styles, UI slots, diagnostic decorations, ANSI color locality, color-free monochrome, and non-color cursor/selection/search/diagnostic carriers.
- [x] Every legacy expected style remains exact; their body remains terminal-default and only separately asserted gutter values are added.
  - Evidence: the pre-existing exact default tables remain unchanged and pass; separate assertions cover terminal-default legacy bodies and isolated gutter additions.
- [x] Each new bundled color theme has reviewed body/gutter/text/accent/status/diagnostic mappings, a body distinct from its gutter in TrueColor and ANSI-16, and curated contrast/anchor tests.
  - Evidence: exact pinned RGB anchors, explicit ANSI rows, body/gutter distinctness, and primary text/status contrast tests pass for Catppuccin Mocha, Dracula, Nord, Solarized Dark, and Tokyo Night. Solarized's approved canonical base0/base02 surface pairing is guarded at its measured 4.11:1 while its body pairing remains above 4.5:1.
- [x] Public API, core dependency direction, color locality, Cargo dependencies, lockfile, and vendor tree remain unchanged.
  - Evidence: no manifest, lockfile, or vendor changes; public-API, dependency-hygiene, hard-coded-color, deny, and audit gates pass.

## Main-thread review

Review found and fixed two cross-theme cursor regressions: colored cursor-line and cursor surfaces initially shared one background, and derived monochrome cursor rows initially lacked the underline carrier. Cursor line now uses the passive surface, the cursor uses the active surface, and shared monochrome lowering preserves bold plus underline. The existing all-theme rendered-cell test confirms the distinction and accessibility behavior.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
