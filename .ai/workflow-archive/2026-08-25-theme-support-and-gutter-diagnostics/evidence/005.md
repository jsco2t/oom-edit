# Evidence — Task 005

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/gutter.rs`
- `crates/oom-edit/src/lib.rs`

## Acceptance criteria

- [x] Shuffled/repeated diagnostics deduplicate correctly and highest severity is exact for every pair/order.
  - Evidence: exhaustive pairwise tests shuffle duplicate severities and assert the explicit Error > Warning > Info > Hint reduction without relying on enum ordering.
- [x] Fixed glyph/role/modifier mapping is exhaustive and independent of theme-authored input.
  - Evidence: table-driven tests cover the complete E/error, W/warning, I/info, H/text-muted mapping and require the application-owned bold modifier for every severity.
- [x] Boundary budgets, partial visibility, atomic completion, cancellation, and stale-generation refusal have deterministic tests.
  - Evidence: unit-budget tests cover zero work, exact incremental progress, unavailable partial output, completion-only publication, matching-generation publication, and stale-generation rejection.
- [x] Memory-shape tests prove compact O(unique marked lines) storage and stable capacity under lookup.
  - Evidence: the immutable snapshot test verifies sorted compact entries, the 24-bytes-per-line plus 4 KiB bound, binary lookup correctness, and unchanged capacity after repeated reads.
- [x] The module is TUI-private, has no terminal/core ownership reversal, and does not add dependencies or background threads.
  - Evidence: the module is crate-private, imports only the core-owned diagnostic severity and ratatui's presentation modifier, stores no core/layout DTOs, and introduces no manifest, lockfile, vendor, or thread changes.

## Main-thread review

Review confirmed the builder keeps partial work private, consumes completed work at the publication boundary, refuses stale generations, uses an explicit severity comparator, and retains only `(source_line, severity)` in the published snapshot. A temporary module-level dead-code allowance keeps the staged pure state warning-free until Task 006 wires every production path.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
