# Evidence — Task 011

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/widgets/status_bar.rs`
- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/event.rs`
- `crates/oom-edit/tests/snapshots/*.txt` (25 intentional gutter/body geometry updates)

## Acceptance criteria

- [x] Line 73 has exact unmarked gutter `  73 ` and exact warning gutter `W 73 `, composing as `  73 |Line of text` and `W 73 |Line of text` respectively.
  - Evidence: `gutter_marker_replaces_leading_alignment_cell_without_moving_number` asserts the complete five-cell marked and unmarked rows.
- [x] The diagnostic marker remains at column zero with unchanged `E`/`W`/`I`/`H` glyph, severity priority, foreground, gutter background, modifier, and monochrome accessibility behavior in source and rendered modes.
  - Evidence: the all-tier marker composition matrix remains exact at the shorter width; App and rendered marker integration tests pass unchanged apart from derived geometry.
- [x] `gutter_width` is exactly 5 cells for documents ending at lines 9, 10, and 100, and 6 cells for line 1000; the absolute/relative number field retains its previous width and alignment.
  - Evidence: `gutter_width_tracks_digit_boundaries` and `gutter_keeps_the_number_field_across_digit_boundaries` assert exact widths and complete label strings.
- [x] Exact absolute, current-line, and signed hybrid-relative matrices at 9/10/999/1000 boundaries contain one trailing content-gap cell and never lose a sign or digit.
  - Evidence: status-bar formatter matrices and `gutter_preserves_relative_digit_sign_and_narrow_geometry` assert full rows including the one-cell suffix.
- [x] Source and rendered body origins move exactly one column left, available text widths gain exactly one column, and cursor, viewport, wrapping, selection/provenance, and horizontal scrolling remain internally consistent.
  - Evidence: exact source and rendered body-origin tests place content at column five; cursor, resize, wrapping, diagnostic projection, selection, provenance, and scrolling assertions were updated to their derived geometry and the complete suite passes.
- [x] Wrapped, synthetic, and below-document rows remain marker-free where applicable and retain complete gutter-background styling; zero and narrow areas remain bounded and panic-free.
  - Evidence: continuation, synthetic-row, clipping, themed-background, and horizontal-scroll matrices all pass at the compact width.
- [x] Updated golden fixtures show only the intended one-column gutter/body shift; no unrelated documentation, API, dependency, lockfile, vendor, theme, license, or evidence changes occur.
  - Evidence: 25 changed TUI goldens were reviewed; numbered and synthetic rows lose one gutter cell, body layouts gain one column, and `git diff --check` passes. No dependency or retained-performance-evidence file changed.

## Main-thread review

The implementation is intentionally centralized: `GUTTER_CONTENT_GAP` changes from two to one, and existing source/rendered consumers derive width and origin from it. Review added an exact absolute/current/relative formatter matrix, updated cursor and viewport expectations that legitimately depend on available body width, and inspected representative wide/narrow/source/rendered golden diffs. No marker logic, state, style, public API, or dependency path changed.

## Task-specific validation

| Command            | Result |
| ------------------ | ------ |
| `make test`        | PASS   |
| `make bench-check` | PASS   |

## Standard quality gate

| Command      | Result |
| ------------ | ------ |
| `make check` | PASS   |

## Remaining concerns

None
