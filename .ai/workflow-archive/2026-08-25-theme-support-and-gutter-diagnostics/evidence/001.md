# Task 001 Evidence: Performance Harness and Pre-change Baseline

Status: complete

## Implementation

- Added a private `App::render`/`TestBackend` performance harness for source and rendered steady frames, distinct cold source/rendered first frames, edit-to-frame, source scrolling, stable repeated rendering, and idle quiescence.
- Added a fixed-schema TSV recorder/comparator with atomic writes, same-host metadata checks, complete one-based trial validation, integer threshold logic, and macOS/Linux process CPU and peak-RSS normalization.
- Added `tui-perf-test`, `tui-perf-record`, and `tui-perf-compare` make workflows and extended `bench-check`, `bench`, `test`, and `check` with the appropriate performance coverage.
- Replaced repeated rendered source-prefix newline scans with one line-start index and exact semantic line-number regression coverage.
- Added 256 KiB, 512 KiB, and 1 MiB cold rendered-layout time/heap scaling gates and deterministic transitive layout-capacity accounting.
- Documented separate NFR-1 source startup and NFR-4 rendered layout/startup, heap, RSS, and scaling contracts in `docs/performance.md` and the authoritative feature PRD.

## Acceptance evidence

- TSV tests cover the fixed header, round trips, control characters, invalid numeric units/values, missing/duplicate/nonconsecutive trials, missing cases, metadata mismatch, exact median/worst/delta aggregates, dual-threshold boundaries, and the rendered RSS boundary.
- `make help` exposes the record/compare workflows; `make bench-check` and `make bench` execute the new TUI cases.
- Release large-layout results:
  - 256 KiB: 45.18 ms average, 45.47 ms worst, 11,717,520 retained bytes.
  - 512 KiB: 90.17 ms average, 91.04 ms worst, 23,434,792 retained bytes.
  - 1 MiB: 179.64 ms average, 180.28 ms worst, 46,869,472 retained bytes.
  - Both doubling steps stay below 2.25×; the 1 MiB case stays below 250 ms and 64 MiB.
- Five-trial fixture-v2 baseline has 35 passing case rows. Source first frame was 108.1–110.5 ms; rendered first frame was 287.7–289.9 ms with approximately 167.4 MB peak RSS and 46,869,472 retained layout bytes.
- Original fixture-v1 failed evidence is retained byte-for-byte at `pre-optimization-baseline.tsv` with SHA-256 `7e01dcc86292e830a0744e082634dc2a1c1ef52e30d9120401c0743b86eeb241`.
- Post-repair/pre-theme baseline is retained at `baseline.tsv` with SHA-256 `70932e6dcfa40352e2f83d6e0cca4c11f7e3613b5f1f59a640e372a1fcfa90fd`.
- No Cargo dependency, lockfile, vendor tree, public API, theme, gutter, or marker production behavior changed.

## Validation

| Command | Result |
| --- | --- |
| `make fmt-check` | passed |
| `cargo test -p oom-edit-core --offline --locked` | passed |
| `make test` | passed |
| `make bench-check` | passed |
| `make bench` | passed |
| `make tui-perf-record BRANCH_ROLE=baseline OUTPUT=/Users/jason/Developer/sources/personal/notebook/projects/oom-edit/features/04-themes/evidence/performance/baseline.tsv TRIALS=5` | passed; 35 rows retained |
| `make check` | passed; 7/7 gates |

