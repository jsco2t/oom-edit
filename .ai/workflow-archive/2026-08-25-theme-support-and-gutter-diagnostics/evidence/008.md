# Evidence — Task 008

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `README.md`
- `docs/performance.md`
- `crates/oom-edit/src/args.rs`
- `crates/oom-edit/src/lib.rs`
- `crates/oom-edit/src/theme.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/perf_tests.rs`
- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/tests/dependency_hygiene.rs`

## Acceptance criteria

- [x] A file-loaded custom dark/light theme crosses loader → catalog → resolver → App → both renderers and persists only the active config slot.
  - Evidence: `file_loaded_dark_and_light_themes_resolve_render_cycle_and_persist` exercises both appearances, exact rendered/source cells, catalog cycling, and active-slot-only persistence.
- [x] Invalid selected and unrelated invalid files prove warning order and matching fallback before terminal construction.
  - Evidence: `rejected_selected_theme_and_unrelated_failure_warn_before_matching_fallback` asserts ordered typed warnings, light-mode fallback, and source ordering before `TerminalGuard::new`.
- [x] Real bounded diagnostics cross provider → pending build → completed snapshot → both gutters and invalidate correctly on edit/tab/lifecycle transitions.
  - Evidence: the complete suite covers bounded publication, cancellation, edit clearing, tab isolation, provider reset, open/replace, and both public render paths.
- [x] Help and README accurately document the complete supported contract and omit excluded/unsupported behavior.
  - Evidence: help now accepts built-in or user names; README lists the exact catalog, strict startup-only schema, limits, selection/fallback/cycling, markers, warnings, and exclusions. The README TOML block is parsed and compared byte-for-byte with the tested complete fixture.
- [x] Registry, provenance, licenses, canonical notices, and CLI output have exact one-to-one compliance coverage; all existing SCOWL guarantees remain.
  - Evidence: the registry-derived data-license checks, mutation tests, notice embedding, and exact pre-terminal `--licenses` assertions pass.
- [x] Compile/source guards prove the public facade, dependency direction, color locality, static/no-network posture, and Makefile discoverability remain intact.
  - Evidence: existing facade/core/color guards pass; a TUI network/direct-plugin/source-loading guard and synchronized performance documentation/Make target test were added.
- [x] No duplicate routing/list, dead implementation, unapproved dependency, lockfile/vendor change, or stale comment remains.
  - Evidence: production lookup/cycling remains solely catalog-owned, Clippy is warning-clean, obsolete theme placeholder wording was removed, and Cargo manifests, `Cargo.lock`, and `vendor/` are unchanged.

## Main-thread review

Reviewed the complete private boundary chain, README/parser parity, startup ordering, license identity mapping, public/dependency surfaces, performance terminology, and comments. One proposed guard was narrowed after it incorrectly treated an existing unreachable `hjkl` transitive as product plugin loading; the final guard enforces the actual product-source and direct-dependency boundary.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make data-license-check` | PASS |
| `make doc` | PASS |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
