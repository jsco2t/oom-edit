# Evidence — Task 010

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/screens/editor.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- `crates/oom-edit/src/app.rs`
- `README.md`

## Acceptance criteria

- [x] A warning on line 73 produces the requested leading-marker geometry without moving the document body.
  - Evidence: `gutter_marker_replaces_leading_alignment_cell_without_moving_number` asserts exact marked `W 73  ` and unmarked `  73  ` six-cell rows; the shared width and text origin are unchanged.
- [x] All severity markers occupy column zero with exact styling at every terminal tier.
  - Evidence: `gutter_composes_background_numbers_and_every_marker_at_all_tiers` asserts `E`, `W`, `I`, and `H` at column zero plus exact foreground, gutter background, and bold modifier under TrueColor, ANSI-16, and Monochrome.
- [x] Absolute and hybrid-relative matrices preserve signs, digits, current styling, and alignment.
  - Evidence: the exact seven-cell matrix now asserts `W  -1  `, `W  10  `, `W+989  `, and `W+990  ` while existing 9/10/999/1000 width and current-number tests remain green.
- [x] Source wrapping, rendered synthetic rows, and horizontal scrolling retain marker provenance.
  - Evidence: the source wrapped-row and rendered table/horizontal-scroll tests now assert markers at column zero only for numbered physical-source rows.
- [x] Clipped gutters are bounded and marker-first.
  - Evidence: exact zero-, one-, and two-column cases are panic-free and produce no write, `W`, and `W ` respectively.
- [x] Gutter width, document origin, cursor/viewports, App publication/invalidation, and render quiescence remain unchanged.
  - Evidence: the implementation changes only span composition in the existing shared painter; all source/rendered geometry, App lifecycle, repeated-render, and performance tests pass.
- [x] Documentation matches the leading-marker contract without unrelated surface changes.
  - Evidence: README now says the fixed marker precedes the aligned line number; Cargo manifests, lockfile, vendor tree, APIs, themes, and retained performance evidence are unchanged.

## Main-thread review

Reviewed every cell slice and style boundary for full and clipped widths. The marker replaces only the first existing alignment cell; the remaining formatter output retains its terminal coordinates, the two trailing content-gap cells remain background-only, and no state or layout calculation moved. No high-confidence defect remained.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |
| `make bench-check` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
