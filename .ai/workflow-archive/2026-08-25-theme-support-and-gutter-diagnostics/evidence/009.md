# Evidence — Task 009

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/perf_tests.rs`
- `scripts/tui_performance.py`
- `scripts/test_tui_performance.py`
- `docs/performance.md`
- External `evidence/performance/candidate.tsv`
- External `evidence/performance/comparison.tsv`

## Acceptance criteria

- [x] Debug and release performance matrices cover empty, sparse, dense 5,000/50,000, projection, cancellation, memory, and quiescence states.
  - Evidence: all 15 TUI cases pass in `make bench-check` and `make bench`; deterministic boundary tests reject exact 1 ms, time, snapshot-memory, and off-screen-scaling violations.
- [x] The final rendered-layout matrix stays within all time, heap, RSS, and per-doubling limits.
  - Evidence: 256 KiB / 512 KiB / 1 MiB cold layouts measured 42.41 / 84.47 / 169.90 ms average, 42.98 / 84.68 / 171.46 ms worst, and 11,717,520 / 23,434,792 / 46,869,472 retained heap bytes. Candidate rendered first-frame median/worst RSS was 167,657,472 / 168,198,144 bytes, below 192 MiB.
- [x] Five final candidate trials use baseline-compatible host, toolchain, fixture, and profile metadata.
  - Evidence: `candidate.tsv` contains one header plus 75 passing rows: five trials for each of 15 cases. The comparator accepted all comparable metadata against the frozen baseline.
- [x] Every legacy relative target and every new absolute, memory, scaling, and quiescence target passes.
  - Evidence: `comparison.tsv` contains only `pass` rows. Source dense-50,000 median/worst render was 122,000 / 239,916 ns; rendered dense-50,000 was 440,709 / 451,708 ns. The 5,000→50,000 median wall/CPU changes were +1.23%/+1.23% in source and -0.85%/-0.92% in rendered mode, all within 25%.
- [x] Completed marker snapshots remain within the retained-memory contract.
  - Evidence: dense 50,000 snapshots retain 800,000 bytes against a 1,204,096-byte limit; sparse snapshots retain 64 bytes against a 4,192-byte limit.
- [x] Projection, cancellation, and final idle behavior remain bounded and quiescent.
  - Evidence: projection median/worst was 31,083 / 48,291 ns and cancellation 11,167 / 16,208 ns, below the 1 ms release limit; behavioral tests prove publication is atomic, cancellation is stale-safe, and repeated rendering cannot grow state.
- [x] The implementation is private, dependency-free, Make-owned, and portable across supported macOS/Linux evidence collection paths.
  - Evidence: test-only App hooks remain crate-private, the Python tool supports platform-specific `/usr/bin/time` and RSS units, no dependency or public API changed, and all entry points are discoverable Make targets.
- [x] No threshold was weakened, no row was discarded, and no rebaseline occurred.
  - Evidence: the approved plan hash remained unchanged, the historical seven-case baseline was consumed directly, and the single five-trial candidate record completed successfully. `make bench` was repeated once only because its first command result was truncated before an exit status/session identifier was available; no candidate evidence had been recorded at that point.

## Main-thread review

Reviewed fixture construction and warming boundaries, measurement duration, absolute status handling, atomic TSV writes, legacy-baseline compatibility, metadata/trial validation, dual-threshold comparisons, new snapshot limits, and 5,000→50,000 wall/CPU scaling. No correctness or enforcement gaps remained.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make bench-check` | PASS |
| `make bench` | PASS |
| `make tui-perf-record BRANCH_ROLE=candidate OUTPUT=…/candidate.tsv TRIALS=5` | PASS — 75/75 rows |
| `make tui-perf-compare BASELINE=…/baseline.tsv CANDIDATE=…/candidate.tsv OUTPUT=…/comparison.tsv` | PASS — all comparison rows |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` | PASS |

## Remaining concerns

None
