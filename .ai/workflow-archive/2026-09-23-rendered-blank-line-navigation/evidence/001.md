# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 4

## Files changed

- `crates/oom-edit-core/src/rendered/mod.rs`
- `crates/oom-edit-core/src/rendered/nav.rs`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/src/style.rs`
- `crates/oom-edit-core/tests/conformance/mod.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/screens/rendered.rs`
- Rendered layout and TUI golden snapshots under `crates/oom-edit-core/tests/goldens/` and `crates/oom-edit/tests/snapshots/`

## Acceptance criteria

- [x] The minimal line-46/47 equivalent has a numbered empty rendered row whose line-level range is the physical blank line and whose atom list is empty.
  - Evidence: `rendered_blank_line_ranges_are_exact_for_lf_crlf_and_unicode` and `rendered_blank_line_preserves_physical_identity_and_canonical_cursor` assert content kind, exact source bytes, line number, empty text, and no atoms.
- [x] Moving down onto the row makes `EditorSession::cursor()` the blank line at column zero and remapping from that canonical position returns to it.
  - Evidence: `rendered_blank_line_preserves_physical_identity_and_canonical_cursor` covers both vertical entry and remapping.
- [x] Absolute and relative gutters render the blank line's correct source number/value instead of an empty cell.
  - Evidence: `rendered_blank_line_gutter_and_status_report_its_physical_position` renders and checks both gutter modes.
- [x] The full status row reports the blank line at display column one.
  - Evidence: the same TUI regression checks the rendered status text contains `2:1`.
- [x] LF, CRLF, Unicode, and adjacent-no-blank cases preserve exact offsets and do not number presentation-only separators.
  - Evidence: core layout tests cover exact LF/CRLF byte ranges with Unicode and retain a synthetic unnumbered separator for adjacent blocks.
- [x] Existing wrapped continuation rows and generated separators remain unnumbered and source-atom-free.
  - Evidence: focused gutter tests, block-contract tests, conformance tests, and updated goldens all pass.

## Main-thread review

The review verified that physical blank ownership is derived only from exact parser-span gaps, remains atom-free, and preserves the single rendered separator policy. Gate failures exposed two compatibility assumptions: paragraph motions recognized only synthetic boundaries, and several cursor/selection tests expected blank rows to retain the previous block's source position. Boundary detection now accepts physical atom-free blank content while preserving synthetic separators, and the affected tests assert the new canonical blank-line identity. No new public API or dependency was introduced.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `cargo test -p oom-edit-core --offline --locked rendered_blank_line` | PASS |
| `cargo test -p oom-edit-core --offline --locked rendered_line_number` | PASS |
| `cargo test -p oom-edit --offline --locked rendered_blank_line` | PASS |
| `cargo test -p oom-edit --offline --locked rendered_gutter` | PASS |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

`None`
