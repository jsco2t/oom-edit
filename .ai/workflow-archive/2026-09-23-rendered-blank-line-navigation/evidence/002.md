# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/rendered/nav.rs`
- `crates/oom-edit-core/tests/session_integration.rs`

## Acceptance criteria

- [x] One `h` and one Left from the blank row each land on the final atom of the final rendered row of the preceding wrapped paragraph.
  - Evidence: `horizontal_navigation_from_blank_line_uses_directional_edges` first failed with `h` landing on rendered row 3 instead of the final atom on row 4, then passed for both inputs after the fix.
- [x] The canonical cursor points to that exact final source character, including with multibyte Unicode near the boundary.
  - Evidence: the regression ends the wrapped paragraph with `東京` and compares the canonical cursor with the final atom's exact source offset.
- [x] Counted backward movement proceeds from that character without an extra row jump, skip, or duplicate step.
  - Evidence: `2h` from the blank row is asserted to land on the atom immediately before the paragraph's final atom.
- [x] Forward movement from atom-free rows is directionally symmetric and both document boundaries clamp safely.
  - Evidence: `l` and Right select the first following atom; generated separators use the correct previous/next edges; first and last source atoms clamp.
- [x] Character Select keeps its active rendered endpoint and source ranges in sync with the corrected motion.
  - Evidence: Select from the blank row followed by `h` asserts the final rendered point and its exact source range.
- [x] Existing horizontal movement from source-backed wrapped rows is unchanged.
  - Evidence: `rendered_horizontal_navigation_crosses_rendered_rows`, Select-focused tests, randomized mapping conformance, and the full gate pass.

## Main-thread review

The directional rule is local to horizontal navigation and leaves the shared nearest-source helper unchanged. Source-backed rows retain their existing adjacent-atom behavior. Atom-free rows consume the first step at the nearest source-backed edge strictly before or after the row; remaining counted steps reuse the established atom traversal and clamp at document bounds. The implementation introduces no allocations, public API changes, or new dependencies.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `cargo test -p oom-edit-core --offline --locked horizontal_navigation_from_blank_line` | PASS |
| `cargo test -p oom-edit-core --offline --locked rendered_horizontal_navigation` | PASS |
| `cargo test -p oom-edit-core --offline --locked rendered_select` | PASS |
| `cargo test -p oom-edit-core --offline --locked random_mode_width_and_motion_mapping_stays_in_bounds` | PASS |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

`None`
