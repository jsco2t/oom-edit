# Evidence — Task 005

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/src/lib.rs`
- `crates/oom-edit-core/tests/public_api.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`

## Acceptance criteria

- [x] Up/Down traverse ten stored submissions, clamp at both ends, and restore the original draft after newest.
- [x] Editing and pasting into a recalled command detaches navigation; stored commands stay unchanged. Escape and blank Enter add no entry; duplicates are retained.
- [x] Tabs share one in-memory handle across opens and reloads. A fresh App or standalone session starts empty.
- [x] No persistence or dependency was added.

## Main-thread review

Core owns the bounded history and Command prompt navigation state. App only passes one shared handle to each newly constructed session. The command text lives only inside Command mode, and submit records before command dispatch, including commands that report errors. The new public handle is re-exported and compile-guarded.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` with the writable local advisory cache and offline scanner flags recorded in Task 001 evidence | PASS: 7 checks, 0 failures, no warnings |

## Remaining concerns

None
