# Evidence — Task 004

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/document.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/tests/public_api.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/lifecycle.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/overlay/palette.rs`

## Acceptance criteria

- [x] `:e!` and `:reload` reload the current file's newest disk text and clear dirty state.
- [x] `:reload-all` preloads every open file before committing any tab, retaining order, active index, and paths.
- [x] Missing, unnamed, and invalid UTF-8 targets leave all tabs and dirty edits unchanged. Reload never applies new-file semantics.
- [x] Existing save, close, and open lifecycle tests pass.

## Main-thread review

Reload is a typed core effect and an App lifecycle action. `EditorSession::open_existing` shares normal decoding but refuses missing files. The App captures tab targets when handling the effect and commits only after every replacement opens. Help metadata and public API guards cover the new surface.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` with the writable local advisory cache and offline scanner flags recorded in Task 001 evidence | PASS: 7 checks, 0 failures |

## Remaining concerns

None
