# Evidence — Task 007

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/command/registry.rs`
- `crates/oom-edit/src/overlay/palette.rs`
- `crates/oom-edit/src/overlay/mod.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/tests/public_api.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/tests/snapshots/palette_filtered.txt`
- `crates/oom-edit/tests/snapshots/palette_top.txt`
- `crates/oom-edit/tests/snapshots/palette_vim_reference.txt`

## Acceptance criteria

- [x] `:wq` opens a prefilled core Command prompt on the first Enter without changing text or running lifecycle work; the next Enter submits through core.
- [x] Escape cancels the prompt without execution or history; a submitted command enters process-local history.
- [x] `:e {path}` and `:tabnew {path}` use editable literal prefixes, omit placeholders, and follow the existing file lifecycle path after submission.
- [x] Enabled App commands still execute immediately; disabled and read-only rows stay open and inert on Enter.
- [x] The handoff works from Normal and all Select shapes, with exclusive modal input.

## Main-thread review

Ex command prefills are explicit metadata on registry or reference rows. App receives a typed palette action and asks the core session to open Command mode; App does not parse or execute ex text. Composite and ambiguous reference rows remain read-only. Core validates prefills and records history only on submission. The task-specific test and repository gate passed without warnings or failures.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` with the writable local advisory cache and offline scanner flags recorded in Task 001 evidence | PASS: 7 checks, 0 failures |

## Remaining concerns

None
