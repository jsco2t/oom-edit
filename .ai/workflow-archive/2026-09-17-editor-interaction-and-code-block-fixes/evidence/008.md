# Evidence — Task 008

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/src/style.rs`
- `crates/oom-edit-core/tests/public_api.rs`
- `crates/oom-edit-core/tests/source_viewport.rs`
- `crates/oom-edit-core/tests/spell_session.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/screens/editor.rs`

## Acceptance criteria

- [x] Source Insert frames and TUI cells show the kitchen-sink Go lines with four- and eight-column indentation; rendered Normal tests remain green.
- [x] Mixed spaces, consecutive tabs, and tabs after wide characters expand to four-column stops while the canonical text and saved bytes retain literal tabs.
- [x] Wrapped rows, unwrapped clipping, cursor placement, App scroll-follow, and viewport cell-to-source lookup use the expanded display geometry.
- [x] Go keyword styles and spell decorations stay aligned after tabs; clipping indicators have no source byte.
- [x] Existing no-tab source behavior, the earlier work-package tests, and the performance suite pass.

## Main-thread review

The core owns a display-only source projection. Expanded spaces keep the original tab byte range, and the TUI renders the returned `SourceFrame`. `Viewport.left_col` remains a source-character offset. App uses core display-column methods for tabbed-line scrolling and retains its previous no-tab path. Review found and fixed a narrow viewport case where the left clipping marker covered a cursor immediately after a tab, and removed a repeated prefix scan from wrapped atom mapping.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` with the writable local advisory cache and offline scanner flags recorded in Task 001 evidence | PASS: 7 checks, 0 failures, no warnings |

## Remaining concerns

None
