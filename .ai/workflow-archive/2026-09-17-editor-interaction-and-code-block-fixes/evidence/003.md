# Evidence — Task 003

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/lib.rs`
- Constructor updates in `crates/oom-edit/src/event.rs` and `perf_tests.rs`

## Acceptance criteria

- [x] `:tabnew ./examples/kitchen-sink.md` is classified as tabnew, not substitution, and opens relative to the captured launch directory.
- [x] Absolute paths, spaces, and `s/` inside path text are preserved. App and core tests cover path identity and file contents.
- [x] Command-mode paste accepts a printable ASCII path and trims clipboard line endings. Embedded newline, NUL, tab, and non-ASCII are rejected before buffer mutation. Overlay paste does not reach the session.
- [x] Substitute, save, and edit parser regressions pass, including paths with spaces and substitution ranges.

## Main-thread review

Reviewed parsing ownership and the path handoff: core recognizes command syntax only at a valid prefix and emits a literal path; App resolves only tabnew paths against its owned launch-directory value. Terminal input remains translated once. No shell evaluation or extra clipboard dependency was added.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` with the writable local advisory cache and offline scanner flags recorded in Task 001 evidence | PASS: 7 checks, 0 failures |

## Remaining concerns

None
