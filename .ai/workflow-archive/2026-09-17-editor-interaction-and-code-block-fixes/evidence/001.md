# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit/src/command/registry.rs`
- `crates/oom-edit/src/command/keymap.rs`
- `crates/oom-edit/src/overlay/palette.rs`
- `crates/oom-edit/src/app.rs`
- Four affected TUI snapshots

## Acceptance criteria

- [x] Plain `?` opens one palette in Normal and every Select shape. App tests exercise character, line, and block Select with plain and Shift-modified `?`.
- [x] Palette focus and Up/Down reach only enabled executable commands. Palette tests inspect the initial Help action, subsequent Save action, and all later navigated actions; App tests execute Help with Enter in Select mode.
- [x] Reference rows remain non-executable and use a distinct marker; disabled commands are excluded from action focus. Registry and palette tests verify both modes show `? / Space h/?`.
- [x] Registry, palette, App, and snapshot regressions are updated and pass.

## Main-thread review

Reviewed the registry projection, palette row construction, selected-row styling, event exclusivity, and snapshot changes. Corrected older tests that assumed the first row was an actionable reference. No parallel binding list was added.

## Task-specific validation

| Command | Result |
| --- | --- |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| --- | --- |
| `make check` with a writable local copy of the existing advisory databases and the Makefile's offline scanner flags | PASS: 7 checks, 0 failures |

The first `make check` reached all code gates but could not lock the read-only home advisory databases. The complete gate passed using `CARGO_HOME=/tmp/oom-edit-cargo-home`, `DENY_FLAGS='check -D warnings --disable-fetch'`, and `AUDIT_FLAGS='-D warnings --no-fetch'`. This scanned the existing local RustSec databases without changing policy or suppressing findings.

## Remaining concerns

None
