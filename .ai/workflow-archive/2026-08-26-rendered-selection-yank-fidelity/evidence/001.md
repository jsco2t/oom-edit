# Evidence — Task 001

## Implementation

- Implemented by: main
- Attempt count: 3

## Files changed

- `crates/oom-edit-core/src/clipboard.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/src/session/live_document.rs`
- `crates/oom-edit-core/src/vim.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`

## Acceptance criteria

- [x] Multiline character yanks preserve physical and blank-line newlines, indentation, ordinary and trailing spaces, and Markdown markers.
  - Evidence: `rendered_character_yank_preserves_multiline_markdown_and_whitespace` and `character_markdown_envelope_preserves_source_only_gaps`.
- [x] Forward and reverse selections match, and soft wrapping preserves source spaces without adding newlines.
  - Evidence: forward/reverse matrix in `rendered_character_yank_preserves_multiline_markdown_and_whitespace`; `rendered_character_yank_preserves_soft_wrap_spaces_without_newlines`.
- [x] Markdown constructs, Unicode, tables, entities, escapes, and ambiguous source provenance retain byte-exact coverage.
  - Evidence: the multiline fixture plus existing code-span, entity, table, Unicode, and provenance regression suites all passed.
- [x] Partial inline selections do not gain unselected boundary delimiters.
  - Evidence: `character_markdown_envelope_does_not_add_partial_boundary_delimiters` and `partial_inline_selection_does_not_add_unselected_delimiters`.
- [x] Linewise and blockwise shape/register semantics remain exact.
  - Evidence: existing line and block conformance, padding, resize, and put tests passed; block Markdown payload padding now mirrors the private Vim register model.
- [x] Unnamed/system register payloads match faithful Markdown and multiline put round-trips the source.
  - Evidence: `projected_yank_records_exact_payload_without_broadening_ranges`, `rendered_multiline_yank_register_round_trips_exact_source`, and the updated App put regression.
- [x] Named and black-hole publication/isolation rules are unchanged.
  - Evidence: `projected_named_and_black_hole_yanks_leave_the_mirrored_system_slot_unchanged` passed.
- [x] Delete/change/indent/outdent geometry and results are unchanged.
  - Evidence: exact projected ranges remain the only mutation input; operator-matrix, wrapped-table, and conformance regressions passed.

## Main-thread review

The review confirmed that exact copy text is carried in a private consumer-owned yank DTO while destructive operators still use the original renderer-projected ranges. It also corrected two stale test assumptions: a wrapped table copy now retains its source space, and internal puts now prove Markdown delimiters remain in the register under either clipboard preference.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

None
