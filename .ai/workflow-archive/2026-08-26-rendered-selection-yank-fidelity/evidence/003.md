# Evidence — Task 003

## Implementation

- Implemented by: main
- Attempt count: 2

## Files changed

- `crates/oom-edit-core/src/rendered/mod.rs`
- `crates/oom-edit-core/src/rendered/tests/blocks.rs`
- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`

## Acceptance criteria

- [x] Whole-surface default `y` copies exact fenced Markdown, including language/info text and both delimiters.
  - Evidence: `rendered_complete_code_fence_yank_preserves_exact_source` covers characterwise and linewise whole-surface yanks and verifies exact clipboard payloads.
- [x] Whole-surface fidelity holds forward and backward for empty/multiline bodies, indentation, trailing spaces, final newlines, supported delimiters, longer fences, language variants, and nested source.
  - Evidence: the complete-fence source matrix covers forward/reverse endpoints, backtick and tilde forms, longer backtick delimiters with info text, known/unknown languages, nested blockquote/list source, and a closing fence without a final newline; `rendered_complete_empty_fence_yanks_without_source_backed_body_atoms` covers empty bodies in both directions, and `rendered_selection_spanning_multiple_complete_fences_yanks_exact_source` covers multiple fully selected regions.
- [x] Content-only characterwise and linewise yanks retain exact body whitespace and omit fence/info bytes.
  - Evidence: `rendered_code_fence_content_only_yanks_do_not_add_delimiters` covers both selection shapes and preserved indentation, trailing spaces, and newlines.
- [x] Whole-surface `y` and `Y` keep exact fenced Markdown in the internal register; `y` follows configuration and `Y` publishes syntax-free body text.
  - Evidence: `rendered_complete_fence_plain_text_yank_keeps_exact_markdown_register` verifies `Y` publication and exact Markdown `p`/`P` insertion; `complete_fence_copy_honors_yank_output_policy` covers configured Markdown `y`, configured plain-text `y`, and one-shot `Y` at the App boundary.
- [x] Empty whole fences produce a real clipboard/register yank.
  - Evidence: `rendered_complete_empty_fence_yanks_without_source_backed_body_atoms` verifies the metadata-only selection creates an exact non-empty payload.
- [x] Blockwise yanks and destructive operator geometry remain unchanged.
  - Evidence: the yank preparation explicitly returns blockwise geometry unchanged; `complete_fence_yank_expansion_does_not_broaden_delete_geometry` verifies delete does not inherit fence expansion, while the full operator and conformance suites remain green.
- [x] Synthetic gutter atoms remain source-less and public API/dependency guards remain green.
  - Evidence: `fenced_layout_retains_private_full_source_regions` verifies exact private fence regions and source-less boundary atoms; public API, dependency hygiene, deny, and audit gates passed with no manifest or lockfile changes.
- [x] The full repository quality gate passes.
  - Evidence: task validation and the independent task gate both passed `make check`.

## Main-thread review

The review verified that fence provenance is captured from parser leaves, cached and invalidated with the matching layout, and applied only to a cloned yank selection. It found and fixed one resize edge case: atomless synthetic fence endpoints now remap by their retained rendered-line identity, preserving complete-fence coverage after a layout rebuild. The retry repaired a Clippy-only nested conditional without behavioral change.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make test` | PASS |
| `make check` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

None
