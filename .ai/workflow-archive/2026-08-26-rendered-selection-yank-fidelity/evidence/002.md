# Evidence — Task 002

## Implementation

- Implemented by: main
- Attempt count: 1

## Files changed

- `crates/oom-edit-core/src/session.rs`
- `crates/oom-edit-core/tests/session_integration.rs`
- `crates/oom-edit/src/app.rs`
- `crates/oom-edit/src/command/registry.rs`
- `crates/oom-edit/src/overlay/palette.rs`
- `crates/oom-edit/tests/snapshots/palette_floor_40x12.txt`
- `crates/oom-edit/tests/snapshots/palette_top.txt`
- `README.md`
- `CHANGELOG.md`

## Acceptance criteria

- [x] `Y` publishes syntax-free multiline text once, exits Select, and does not edit the document.
  - Evidence: `rendered_plain_text_yank_is_one_shot_and_keeps_markdown_register` and `uppercase_y_copies_plain_text_once_and_keeps_markdown_for_put`.
- [x] `Y` is independent of both global format preferences while `y` retains the configured policy and Markdown default.
  - Evidence: App matrix covers both `ClipboardCopyFormat` variants; the existing configured-`y` matrix remains green.
- [x] Internal puts after `Y` use faithful Markdown.
  - Evidence: both core and crossterm-to-App tests assert source-preserving put results.
- [x] Register prefixes preserve system publication and named/black-hole isolation.
  - Evidence: `select_plain_text_yank_preserves_register_publication_rules` and `link_index_yank_keys_keep_named_and_black_hole_isolation`.
- [x] Clipboard success, failure, and the 100 KiB limit apply to the actual plain-text output.
  - Evidence: `uppercase_y_copies_plain_text_once_and_keeps_markdown_for_put`, `uppercase_y_reports_clipboard_failures`, and `uppercase_y_size_limit_applies_to_plain_text_payload`.
- [x] Synthetic-only links remain safe and format-invariant for both yank keys.
  - Evidence: expanded link-index Select tests cover `y`, `Y`, Enter, and isolated register prefixes.
- [x] Registry, palette/help, binding contract, and context projections expose both actions without routing drift.
  - Evidence: the 32-row exact registry contract, all-binding dispatch test, palette projection test, and updated palette goldens passed.
- [x] README and changelog document the default/configured `y` policy and one-shot `Y`.
  - Evidence: updated key table, Clipboard section, size-limit guidance, and Unreleased entries.
- [x] No public API or dependency changes were introduced.
  - Evidence: public API, dependency hygiene, deny, and audit gates passed; Cargo manifests and lockfile are unchanged.

## Main-thread review

The review verified that `Y` remains core-owned, accepts crossterm's uppercase forms with or without Shift, rejects Ctrl/Alt variants, and changes only the outgoing clipboard representation. Registry wording was refined to state that Markdown is the default without misdescribing configured plain-text `y` behavior. Synthetic link copies were aligned with named and black-hole publication isolation.

## Task-specific validation

| Command | Result |
| ------- | ------ |
| `make test` | PASS |

## Standard quality gate

| Command | Result |
| ------- | ------ |
| `make check` | PASS |

## Remaining concerns

None
